<Window x:Class="NV.CT.NP.Tools.PasswordGenerator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NV.CT.NP.Tools.PasswordGenerator"
        mc:Ignorable="d" FontSize="16"
        Title="Password Encrypted Tool" Height="300" Width="800">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="VerticalContentAlignment" Value="Center"></Setter>
            <Setter Property="Margin" Value="0 0 10 0"></Setter>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"></Setter>
            <Setter Property="Margin" Value="0 0 10 0"></Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="10" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"></RowDefinition>
            <RowDefinition Height="60"></RowDefinition>
            <RowDefinition Height="60"></RowDefinition>
        </Grid.RowDefinitions>
	    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBlock Width="200">Plain Password:</TextBlock>
            <TextBox x:Name="PlainPassword" Width="320"></TextBox>
            <Button Content="Encrypt" Width="100" x:Name="BtnEncrypt" Click="BtnEncrypt_OnClick"></Button>
		</StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
            <TextBlock Width="200">Encrypted Password:</TextBlock>
            <TextBlock x:Name="EncryptedPassword" Width="320"></TextBlock>
            <Button Content="copy to clipboard" Width="200" x:Name="BtnCopy" Click="BtnCopy_OnClick"></Button>
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="10">
            <TextBlock Width="200"></TextBlock>
            <TextBlock x:Name="TxtMessage" Width="300"></TextBlock>
        </StackPanel>
    </Grid>
</Window>
