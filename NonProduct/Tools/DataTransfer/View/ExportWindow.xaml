<Window x:Class="NV.CT.NP.Tools.DataTransfer.View.ExportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NV.CT.NP.Tools.DataTransfer.View"
        mc:Ignorable="d"
        Title="临床数据传输" Height="200" Width="400" 
        WindowStyle="ToolWindow" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" 
        Closed="Window_Closed" 
        ShowInTaskbar="False">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="导出进度"/>
        <local:LoadingControl x:Name="loader" Grid.Row="1" Height="80" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <TextBlock Text="正在导出："/>
            <TextBlock d:Text="patient 1" Text="{Binding PatientName}"/>
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal">
            <TextBlock Text="剩余未导出病人数量："/>
            <TextBlock d:Text="10" Text="{Binding RemainCount}"/>
            <TextBlock Text="已选中的全部病人总数：" Margin="10 0 0 0"/>
            <TextBlock d:Text="100" Text="{Binding TotalCount}"/>
        </StackPanel>
    </Grid>
</Window>
