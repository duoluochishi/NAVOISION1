<Window
	x:Class="NV.CT.UI.Controls.Controls.LoadingWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:gif="http://wpfanimatedgif.codeplex.com"
	xmlns:local="clr-namespace:NV.CT.UI.Controls.Controls"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	Title=""
	Width="260"
	Height="110"
	d:DesignHeight="600"
	d:DesignWidth="800"
	AllowsTransparency="True"
	Background="Transparent"
	BorderThickness="0"
	ShowInTaskbar="False"
	Topmost="True"
	WindowStartupLocation="Manual"
	WindowStyle="None"
	mc:Ignorable="d">
	<Window.Resources>
		<Style
			x:Key="FIcon"
			TargetType="TextBlock">
			<Setter Property="FontFamily" Value="/NV.CT.UI.Controls;component/Fonts/#SF2015" />
			<Setter Property="Foreground" Value="White" />
			<Setter Property="TextAlignment" Value="Center" />
			<Setter Property="HorizontalAlignment" Value="Center" />
			<Setter Property="VerticalAlignment" Value="Center" />
			<Setter Property="FontSize" Value="20" />
		</Style>
	</Window.Resources>
	<Grid>
		<Border
			Background="#16162B"
			CornerRadius="1"
			Opacity="0.89" />
		<StackPanel
			Margin="5"
			HorizontalAlignment="Center"
			VerticalAlignment="Center"
			Orientation="Horizontal">
			<TextBlock
				x:Name="FIcon"
				Margin="3"
				FontSize="50"
				RenderTransformOrigin="0.5,0.5"
				Style="{StaticResource FIcon}"
				Text="&#xe65f;">
				<TextBlock.RenderTransform>
					<RotateTransform x:Name="TransFIcon" Angle="0" />
				</TextBlock.RenderTransform>

			</TextBlock>
			<TextBlock
				x:Name="txtMessage"
				Width="160"
				Margin="2,10,15,10"
				VerticalAlignment="Center"
				Foreground="White"
				TextWrapping="Wrap">
				Loading...
			</TextBlock>
		</StackPanel>
	</Grid>
	<Window.Triggers>
		<EventTrigger RoutedEvent="Window.Loaded">
			<BeginStoryboard>
				<Storyboard>
					<DoubleAnimation
						RepeatBehavior="Forever"
						Storyboard.TargetName="TransFIcon"
						Storyboard.TargetProperty="Angle"
						To="360"
						Duration="0:0:2.5" />
				</Storyboard>
			</BeginStoryboard>
		</EventTrigger>
	</Window.Triggers>
</Window>
