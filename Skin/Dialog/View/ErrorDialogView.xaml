<UserControl  x:Class="NV.MPS.UI.Dialog.View.ErrorDialogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"   
    Width="480" Height="280" d:DesignHeight="200" d:DesignWidth="300" mc:Ignorable="d">
    <UserControl.Resources>
        <SolidColorBrush x:Key="ButtonBackColor" Color="#5a5a89" Opacity="0.28"/>
        <SolidColorBrush x:Key="ButtonFontColor" Color="White"  />
        <Style x:Key="BoundingBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#3c4354"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#5a5a89"/>
            <Setter Property="CornerRadius" Value="5"/>
        </Style>
        <Style x:Key="BorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#5a5a89"/>
            <Setter Property="Opacity" Value="0.3"/>
            <Setter Property="CornerRadius" Value="5 5 0 0"/>
        </Style>
        <Style x:Key="TileTextBlockStyle" TargetType="TextBlock" BasedOn="{StaticResource  MaterialDesignTextBlock}">
            <Setter Property="Margin" Value="10 0 0 0"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="16px"/>
            <Setter Property="Foreground" Value="{StaticResource BaseForegroundColor}" />
        </Style>
        <Style x:Key="MessageImageStyle" TargetType="Image">           
            <Setter Property="Width" Value="60"/>
            <Setter Property="Height" Value="60"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Opacity" Value="0.9"/>            
        </Style>
        <Style x:Key="MessageInfoLabelStyle" BasedOn="{StaticResource WindowsBorder}" TargetType="{x:Type Border}">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="5"/>
            <Setter Property="Margin" Value="0 5 0 0"/>
        </Style>
        <Style x:Key="MessageInfoStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="5 5 0 0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="LineHeight" Value="18"/>
            <Setter Property="FontSize" Value="14px"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Hidden"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Visible"/>
            <Setter Property="TextWrapping" Value="WrapWithOverflow"/>
        </Style>
        <Style x:Key="OKButtonStyle" TargetType="Button" BasedOn="{StaticResource  MaterialDesignRaisedButton}">
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="37"/>
            <Setter Property="Margin" Value="0 5 10 0"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Content" Value="OK"/>
            <Setter Property="Background" Value="{StaticResource ButtonBackColor}"/>
        </Style>
    </UserControl.Resources>
    <Border Style="{StaticResource WindowsBorder}" CornerRadius="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="12*" />
                <RowDefinition Height="70*" />
                <RowDefinition Height="18*" />
            </Grid.RowDefinitions>
            <Border Style="{StaticResource BorderStyle}" />
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="70*"/>
                    <ColumnDefinition Width="30*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Style="{StaticResource TileTextBlockStyle}" Text="{Binding Title}" />
                <Button Grid.Column="1" Style="{StaticResource TileCloseButtonStyle}" Command="{Binding CloseCommand}">
                    <Path Style="{StaticResource TileClosePathStyle}" />
                </Button>
            </Grid>
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="40*"/>
                    <RowDefinition Height="60*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30*" />
                    <ColumnDefinition Width="70*" />
                </Grid.ColumnDefinitions>
                <Image Grid.Row="0" Grid.Column="0" Style="{StaticResource MessageImageStyle}" Source="{Binding InfoImage}" />
                <TextBlock Grid.Row="0" Grid.Column="1" Style="{StaticResource MessageInfoStyle}" Text="{Binding Content}" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource MessageInfoStyle}" HorizontalAlignment="Center" Text="{Binding Solution}"/>
            </Grid>
            <Button Grid.Row="2" Style="{StaticResource OKButtonStyle}" Command="{Binding OKCommand}" />
        </Grid>
    </Border>
</UserControl>