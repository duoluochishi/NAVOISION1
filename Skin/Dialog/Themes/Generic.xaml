<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:NV.MPS.UI.Dialog">

    <SolidColorBrush x:Key="ButtonBackColor" Color="#5a5a89" Opacity="0.28"/>
    <SolidColorBrush x:Key="ButtonFontColor" Color="White"  />
    <Style x:Key="BoundingBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="#3c4354"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="#5a5a89"/>
        <Setter Property="CornerRadius" Value="5"/>
    </Style>
    <Style x:Key="LocalBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="#5a5a89"/>
        <Setter Property="Opacity" Value="0.3"/>
        <Setter Property="CornerRadius" Value="5 5 0 0"/>
    </Style>
    <Style x:Key="TileTextBlockStyle" TargetType="TextBlock" BasedOn="{DynamicResource MaterialDesignTextBlock}">
        <Setter Property="Margin" Value="10 0 0 0"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="16px"/>
        <Setter Property="Foreground" Value="{DynamicResource BaseForegroundColor}" />
    </Style>

    <Style TargetType="{x:Type local:BaseCustomWindow}">
        <Setter Property="ResizeMode" Value="NoResize" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="AllowsTransparency" Value="true" />
        <Setter Property="ShowInTaskbar" Value="False" />
        <Setter Property="WindowStyle" Value="None" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:BaseCustomWindow}">

                    <Border Style="{DynamicResource  WindowsBorder}" CornerRadius="10">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="36" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Border Style="{StaticResource LocalBorderStyle}" />
                            <Grid x:Name="TitleGrid" Grid.Row="0" VerticalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Style="{DynamicResource TileTextBlockStyle}" Text="{TemplateBinding Title}" Margin="10 0 0 0" VerticalAlignment="Center"/>
                                <Button Grid.Column="1" Style="{DynamicResource TileCloseButtonStyle}" Name="CloseButton" 
                                        Command="{Binding Source={x:Static SystemCommands.CloseWindowCommand}}">
                                    <Path Style="{DynamicResource TileClosePathStyle}" />
                                </Button>
                            </Grid>

                            <AdornerDecorator Grid.Row="1" KeyboardNavigation.IsTabStop="False">
                                <ContentControl Content="{TemplateBinding Content}" x:Name="MainContentPresenter"/>
                            </AdornerDecorator>

                        </Grid>
                    </Border>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
