<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style
        x:Key="StandardGroupBoxStyle"
        TargetType="{x:Type GroupBox}"
        BasedOn="{StaticResource MaterialDesignGroupBox}">
        <Setter Property="HeaderTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock
                        FontSize="16"
                        FontWeight="Bold"
                        Text="{Binding Path=Header, RelativeSource={RelativeSource AncestorType=GroupBox}}" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style
        x:Key="ParameterGroupBoxStyle"
        TargetType="{x:Type GroupBox}"
        BasedOn="{StaticResource {x:Type GroupBox}}">
        <Setter Property="Padding" Value="4 2" />
        <Setter Property="Margin" Value="1" />
        <Setter Property="HeaderTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock
                        HorizontalAlignment="Left"
                        Text="{Binding Header, RelativeSource={RelativeSource AncestorType={x:Type GroupBox}}}" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style
        x:Key="ImageControlScrollBarStyle"
        TargetType="{x:Type ScrollBar}"
        BasedOn="{StaticResource MaterialDesignScrollBar}">
        <Setter Property="Margin" Value="3,3,0,3" />
        <Setter Property="md:ScrollBarAssist.ThumbCornerRadius" Value="2" />
        <Setter Property="md:ScrollBarAssist.ButtonsVisibility" Value="Collapsed" />
        <Setter Property="md:ScrollBarAssist.ThumbWidth" Value="16" />
        <Setter Property="Orientation" Value="Vertical" />
        <Setter Property="Background" Value="{StaticResource BaseBackgroundColor}" />
    </Style>

    <Style
        x:Key="ImageMutexActionToggleButtonStyle"
        TargetType="{x:Type ToggleButton}"
        BasedOn="{StaticResource MaterialDesignChoiceChipOutlineToggleButton}">
        <Setter Property="Margin" Value="1,0" />
        <Setter Property="Width" Value="50" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Style.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Background" Value="Green" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style
        x:Key="ImageNormalActionButtonStyle"
        TargetType="{x:Type Button}"
        BasedOn="{StaticResource MaterialDesignOutlinedButton}">
        <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}" />
        <Setter Property="BorderBrush" Value="#88C6C6EC" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0.4" />
        <Setter Property="md:ButtonAssist.CornerRadius" Value="5" />
        <Setter Property="Margin" Value="1,0" />
        <Setter Property="Width" Value="50" />
    </Style>

</ResourceDictionary>