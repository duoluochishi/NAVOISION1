<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
                    xmlns:calc="clr-namespace:CalcBinding;assembly=CalcBinding"
                    xmlns:logic="clr-namespace:NV.CT.Service.AutoCali.UI.Logic"
                    xmlns:views="clr-namespace:NV.CT.Service.AutoCali.UI">    
    <DataTemplate x:Key="dgTextBlockColumnDataTemplate">
        <TextBlock Text="{Binding Path=.}"></TextBlock>
    </DataTemplate>

    <DataTemplate x:Key="customerDataGridTextColumnDataTemplate">
        <TextBox Text="{Binding Path=Value}"></TextBox>
    </DataTemplate>

    <DataTemplate x:Key="customerDataGridComboboxColumnDataTemplate">
        <ComboBox ItemsSource="{Binding Path=SKUFieldValues}"
                  DisplayMemberPath="FieldValue"
                  SelectedItem="{Binding SelectedUISKUFieldValue}">
        </ComboBox>
    </DataTemplate>

    <DataTemplate x:Key="processStatusTemplate">
        <views:ProcessStatusControl/>
    </DataTemplate>

    <DataTemplate x:Key="caliProjectTemplate">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <views:ProcessStatusControl/>
            <CheckBox Grid.Column="1" IsChecked="{Binding IsChecked}" VerticalContentAlignment="Center" Padding="5,0"
                      IsEnabled="{calc:Binding '!(CaliTaskState == logic:CaliTaskState.WaitingToRun || CaliTaskState == logic:CaliTaskState.Running)'}">
            </CheckBox>
            <TextBlock Grid.Column="2" Text="{Binding Inner.Name}"/>
        </Grid>
    </DataTemplate>

    <!--使用Path生成的图标-->
    <Style x:Key="basePathIconStyle" TargetType="Path">
        <Setter Property="Width" Value="20"/>
        <Setter Property="Stretch" Value="Uniform"/>
    </Style>

    <Style x:Key="plusPathIconStyle" TargetType="Path" BasedOn="{StaticResource basePathIconStyle}">
        <Setter Property="Fill" Value="Gray"/>
        <Setter Property="Data" Value="M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z"/>
    </Style>

    <!--style for GridSplitter-->
    <Style x:Key="CommonGridSplitterStyle" TargetType="GridSplitter" BasedOn="{StaticResource {x:Type GridSplitter}}">
        <Setter Property="ShowsPreview" Value="True"/>
    </Style>

    <Style x:Key="HorizontalGridSplitterStyle" TargetType="GridSplitter" BasedOn="{StaticResource CommonGridSplitterStyle}">
        <Setter Property="Width" Value="3"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
    </Style>

    <Style x:Key="VerticalGridSplitterStyle" TargetType="GridSplitter" BasedOn="{StaticResource CommonGridSplitterStyle}">
        <Setter Property="Height" Value="3"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

</ResourceDictionary>