<UserControl  x:Class="NV.CT.Service.AutoCali.UI.GeneralArgProtocolDataGrid"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
          xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
          mc:Ignorable="d" 
          VerticalAlignment="Top" MinHeight="50"
          d:DesignHeight="450" d:DesignWidth="800">
    <DataGrid AutoGenerateColumns="False" CanUserAddRows="False" CanUserSortColumns="False" CanUserReorderColumns="False"
              VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
              IsReadOnly="True" LoadingRow="dataGrid_LoadingRow" ItemsSource="{Binding Path=CurrentArgProtocolViewModels}"
              SelectedItem="{Binding Path=SelectedArgProtocolViewModel}">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Study" Binding="{Binding Path=Study}" IsReadOnly="True"/>
            <DataGridTextColumn Header="RawDataType" Binding="{Binding Path=RawDataType}" IsReadOnly="True"/>

            <DataGridTextColumn Header="kV" Binding="{Binding Path=KV}" IsReadOnly="True"/>
            <DataGridTextColumn Header="mA" Binding="{Binding Path=MA}" IsReadOnly="True"/>

            <DataGridTextColumn Header="BowtieEnable" Binding="{Binding Path=BowtieEnable}" IsReadOnly="True"/>
            <DataGridTextColumn Header="GainMode" Binding="{Binding Path=GainMode}" IsReadOnly="True"/>

            <DataGridTextColumn Header="ExposureMode" Binding="{Binding Path=ExposureMode}" IsReadOnly="True"/>
            <DataGridTextColumn Header="ExposureTime" Binding="{Binding Path=ExposureTime}" IsReadOnly="True"/>
            <DataGridTextColumn Header="FrameTime" Binding="{Binding Path=FrameTime}" IsReadOnly="True"/>

            <DataGridTextColumn Header="TotalFrames" Binding="{Binding Path=TotalFrames}" IsReadOnly="True"/>
            <DataGridTextColumn Header="FramesPerCycle" Binding="{Binding Path=FramesPerCycle}" IsReadOnly="True"/>
            <DataGridTextColumn Header="CollimitorZ" Binding="{Binding Path=CollimitorZ}" IsReadOnly="True"/>

            <DataGridComboBoxColumn Header="FramePerCycle" ItemsSource="{Binding Path=.}" IsReadOnly="True"/>
        </DataGrid.Columns>

    </DataGrid>

</UserControl>
