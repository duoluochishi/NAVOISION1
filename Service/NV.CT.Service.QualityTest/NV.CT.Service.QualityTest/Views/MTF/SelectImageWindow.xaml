<dialog:BaseCustomWindow
    x:Class="NV.CT.Service.QualityTest.Views.MTF.SelectImageWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:NV.CT.Service.QualityTest.Views.MTF"
    xmlns:dialog="clr-namespace:NV.MPS.UI.Dialog;assembly=NV.MPS.UI.Dialog"
    xmlns:res="clr-namespace:NV.CT.Service.Common.Resources;assembly=NV.CT.Service.Common"
    mc:Ignorable="d"
    Title="Select"
    Height="150"
    Width="450"
    WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="10" />
            <RowDefinition />
            <RowDefinition Height="10" />
        </Grid.RowDefinitions>
        <TextBlock
            Grid.Row="1"
            HorizontalAlignment="Center"
            Text="{x:Static res:Quality_Lang.Quality_UC_SelectImage}" />
        <StackPanel
            Grid.Row="3"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <Button
                x:Name="ButtonLeft"
                Style="{StaticResource ServiceCommonButton}"
                Content="{x:Static res:Quality_Lang.Quality_UC_Left}"
                Click="ButtonLeft_Click" />
            <Button
                x:Name="ButtonCenter"
                Margin="10 0 0 0"
                Style="{StaticResource ServiceCommonButton}"
                Content="{x:Static res:Quality_Lang.Quality_UC_Center}"
                Click="ButtonCenter_Click" />
            <Button
                x:Name="ButtonRight"
                Margin="10 0 0 0"
                Style="{StaticResource ServiceCommonButton}"
                Content="{x:Static res:Quality_Lang.Quality_UC_Right}"
                Click="ButtonRight_Click" />
        </StackPanel>
    </Grid>
</dialog:BaseCustomWindow>