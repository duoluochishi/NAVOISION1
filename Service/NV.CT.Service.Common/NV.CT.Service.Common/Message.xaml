<Window x:Class="NV.CT.Service.Common.Message"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NV.CT.Service.Common"
        mc:Ignorable="d"
        Title="Message" Height="250" Width="300"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Thickness x:Key="ButtonMargin" Left="0" Right="10" Top="0" Bottom="0"/>

        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignOutlinedButton}">
            <Setter Property="Margin" Value="{StaticResource ButtonMargin}"/>
        </Style>
    </Window.Resources>
    <Border Background="{DynamicResource MaterialDesignPaper}" BorderThickness="1" CornerRadius="0" BorderBrush="#5a5a89">
        <Grid Background="Transparent">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="txt" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            <DockPanel Grid.Row="1" LastChildFill="False"
                   Margin="10">
                <Button Content="Cancel" DockPanel.Dock="Right" Margin="5 0"
                    Click="CancelButton_Click"/>
                <Button Content="OK" DockPanel.Dock="Right"
                    Click="Button_Click"/>
            </DockPanel>
        </Grid>
    </Border>
</Window>
