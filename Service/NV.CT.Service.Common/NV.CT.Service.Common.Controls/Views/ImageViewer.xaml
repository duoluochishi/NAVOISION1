<UserControl x:Class="NV.CT.Service.Common.Controls.Views.ImageViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:foundation="clr-namespace:NV.CT.Service.Common.Controls.Models.Foundations"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid
        Grid.Column="1"
        Margin="2,0">
        <Grid.Resources>
            <Style
                x:Key="ImageControlScrollBarStyle"
                BasedOn="{StaticResource MaterialDesignScrollBar}"
                TargetType="{x:Type ScrollBar}">

                <Setter Property="Margin" Value="3,3,0,3" />
                <Setter Property="md:ScrollBarAssist.ThumbCornerRadius" Value="2" />
                <Setter Property="md:ScrollBarAssist.ButtonsVisibility" Value="Collapsed" />
                <Setter Property="md:ScrollBarAssist.ThumbWidth" Value="16" />
                <Setter Property="Orientation" Value="Vertical" />
                <Setter Property="Background" Value="{StaticResource BaseBackgroundColor}" />

            </Style>

        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="46" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>

        <Border
            Grid.ColumnSpan="2"
            Background="{StaticResource PrimaryHueLightBrush}">

            <DockPanel LastChildFill="False">

                <!--  MenuBar  -->
                <Menu DockPanel.Dock="Left">

                    <Menu.Resources>

                        <DataTemplate x:Key="MenuItemBoldHeaderTemplate">
                            <TextBlock Text="{Binding Header, RelativeSource={RelativeSource AncestorType=MenuItem}}" />
                        </DataTemplate>

                    </Menu.Resources>

                    <MenuItem Margin="1,0">
                        <MenuItem.Header>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock
                                    Padding="5,0"
                                    FontWeight="Bold"
                                    Text="File" />
                                <md:PackIcon
                                    Width="20"
                                    Margin="5,0,0,0"
                                    Kind="FileDocumentMultipleOutline" />
                            </StackPanel>
                        </MenuItem.Header>

                        <MenuItem
                            Background="Transparent"
                            Command="{Binding LoadRawDataSeriesFolderCommand}"
                            Header="Load RawData Series"
                            HeaderTemplate="{StaticResource MenuItemBoldHeaderTemplate}" />
                        <!--<MenuItem
                            Background="Transparent"
                            Command="{Binding LoadDicomSeriesFolderCommand}"
                            Header="Load Dicom Series"
                            HeaderTemplate="{StaticResource MenuItemBoldHeaderTemplate}" />-->
                    </MenuItem>

                    <MenuItem Margin="1,0">
                        <MenuItem.Header>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock
                                    Padding="5,0"
                                    FontWeight="Bold"
                                    Text="Image" />
                                <md:PackIcon
                                    Margin="5,0,0,0"
                                    VerticalAlignment="Center"
                                    Kind="HandSaw" />
                            </StackPanel>
                        </MenuItem.Header>

                        <MenuItem
                            Background="Transparent"
                            Command="{Binding ExecuteImageCutCommand}"
                            Header="Image Cut Action"
                            HeaderTemplate="{StaticResource MenuItemBoldHeaderTemplate}"
                            IsEnabled="{Binding ImageViewerManager.CanRunRawDataActions}" />
                        <MenuItem
                            Background="Transparent"
                            Command="{Binding ExecuteImageSortCommand}" CommandParameter="{Binding}"
                            Header="Image Sort Action"
                            HeaderTemplate="{StaticResource MenuItemBoldHeaderTemplate}"
                            IsEnabled="{Binding ImageViewerManager.CanRunRawDataActions}" />
                    </MenuItem>

                    <MenuItem Margin="1,0">
                        <MenuItem.Header>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock
                                    Padding="5,0"
                                    FontWeight="Bold"
                                    Text="Tools" />
                                <md:PackIcon
                                    Margin="5,0,0,0"
                                    VerticalAlignment="Center"
                                    Kind="Toolbox" />
                            </StackPanel>
                        </MenuItem.Header>

                        <MenuItem
                            Background="Transparent"
                            Command="{Binding ExecuteAnalysisToolCommand}" CommandParameter="{Binding}"
                            Header="Analysis Tool"
                            HeaderTemplate="{StaticResource MenuItemBoldHeaderTemplate}"
                            IsEnabled="{Binding ImageViewerManager.CanRunRawDataActions}" />
                    </MenuItem>

                </Menu>

                <TextBlock
                    Margin="20,0,0,0"
                    VerticalAlignment="Center"
                    FontWeight="Bold"
                    Text="Loaded :" />

                <TextBlock
                    Margin="20,0,0,0"
                    VerticalAlignment="Center"
                    FontWeight="Bold"
                    Text="{Binding ImageViewerManager.ImageLoadMessage}" />

                <!--  ImageControl Toolbox  -->
                <StackPanel
                    Margin="0,0,5,0"
                    DockPanel.Dock="Right"
                    Orientation="Horizontal">

                    <StackPanel.Resources>

                        <Style
                            BasedOn="{StaticResource MaterialDesignChoiceChipOutlineToggleButton}"
                            TargetType="{x:Type ToggleButton}">

                            <Setter Property="Margin" Value="1,0" />
                            <Setter Property="Width" Value="50" />
                            <Setter Property="HorizontalAlignment" Value="Left" />
                            <Setter Property="VerticalAlignment" Value="Center" />

                            <Style.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter Property="Background" Value="Green" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>

                        <Style
                            x:Key="ImageNormalActionButtonStyle"
                            BasedOn="{StaticResource MaterialDesignOutlinedButton}"
                            TargetType="{x:Type Button}">

                            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource MaterialDesignBody}" />
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="BorderThickness" Value="1" />
                            <Setter Property="md:ButtonAssist.CornerRadius" Value="5" />
                            <Setter Property="Margin" Value="1,0" />
                            <Setter Property="Width" Value="50" />

                        </Style>

                    </StackPanel.Resources>

                    <ItemsControl ItemsSource="{Binding ImageViewerManager.ImageViewerActions}">

                        <ItemsControl.Resources>

                            <DataTemplate DataType="{x:Type foundation:ImageViewerMutexAction}">
                                <ToggleButton
                                    Command="{Binding DataContext.ImageViewerManager.ExecuteImageViewerMutexActionCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}"
                                    IsChecked="{Binding IsChecked, Mode=TwoWay}">
                                    <md:PackIcon
                                        Width="19"
                                        Height="19"
                                        Kind="{Binding Icon}" />
                                </ToggleButton>
                            </DataTemplate>

                            <DataTemplate DataType="{x:Type foundation:ImageViewerNormalAction}">
                                <Button
                                    Command="{Binding DataContext.ImageViewerManager.ExecuteImageViewerNormalActionCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource ImageNormalActionButtonStyle}">
                                    <md:PackIcon
                                        Width="19"
                                        Height="19"
                                        Kind="{Binding Icon}" />
                                </Button>
                            </DataTemplate>

                        </ItemsControl.Resources>

                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>

                    </ItemsControl>

                </StackPanel>

            </DockPanel>

        </Border>

        <ContentControl
            Grid.Row="2"
            Margin="1,1,0,3"
            Content="{Binding ImageViewerManager.ImageViewer.XWindowsFormsHost, Mode=TwoWay}">

            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Loaded">
                    <i:InvokeCommandAction
                        Command="{Binding ImageViewerManager.ImageContentControlLoadedCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=ContentControl}}" />
                </i:EventTrigger>
                <i:EventTrigger EventName="SizeChanged">
                    <i:InvokeCommandAction
                        Command="{Binding ImageViewerManager.ImageContentControlSizeChangedCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=ContentControl}}" />
                </i:EventTrigger>
            </i:Interaction.Triggers>

        </ContentControl>

        <ScrollBar
            Grid.Row="1"
            Grid.Column="1"
            Maximum="{Binding ImageViewerManager.LoadedImageCount, Mode=TwoWay}"
            Minimum="1"
            Style="{StaticResource ImageControlScrollBarStyle}"
            Thumb.DragDelta="ImageControlScrollBar_DragDelta"
            Value="{Binding ImageViewerManager.CurrentImageIndex, Mode=TwoWay}" />

    </Grid>

</UserControl>
