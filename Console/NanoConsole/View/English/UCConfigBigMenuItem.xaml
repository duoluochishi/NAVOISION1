<UserControl x:Class="NV.CT.NanoConsole.View.English.UCConfigBigMenuItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NV.CT.NanoConsole.View.English"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>

    </UserControl.Resources>
    <Border Name="borderName" BorderBrush="#16162B" BorderThickness="1" MouseEnter="Border_MouseEnter"
            MouseLeave="BorderName_MouseLeave"  Margin="20 14 20 14"  Background="#16162B"  CornerRadius="10"  PreviewMouseDown="Grid_PreviewMouseDown">
        <Grid  Margin="15" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="100" />
                <RowDefinition Height="100" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180" />
                <ColumnDefinition Width="280" />
            </Grid.ColumnDefinitions>
            <Viewbox Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Height="60" Width="60" x:Name="viewbox">
                <Path
                    x:Name="path" Fill="#5A5A89"
                    Stretch="Uniform" Data="{Binding RelativeSource={RelativeSource Mode=FindAncestor,AncestorType={x:Type local:UCConfigBigMenuItem}},Path=Icon}">
                </Path>
            </Viewbox>
            <Label Grid.Row="0" Grid.Column="1" Name="lblName" Foreground="#5A5A89"  FontFamily="Microsoft YaHei" FontSize="15" VerticalAlignment="Bottom" Content="{Binding RelativeSource={RelativeSource Mode=FindAncestor,AncestorType={x:Type local:UCConfigBigMenuItem}},Path=Title}"></Label>
            <Label Grid.Row="1" Grid.Column="1" Name="lblDescribe" Foreground="#5A5A89" FontSize="12" VerticalAlignment="Top" Opacity="0.6" Content="{Binding RelativeSource={RelativeSource Mode=FindAncestor,AncestorType={x:Type local:UCConfigBigMenuItem}},Path=Description}"></Label>
        </Grid>
    </Border>
</UserControl>
