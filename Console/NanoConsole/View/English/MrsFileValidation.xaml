<UserControl x:Class="NV.CT.NanoConsole.View.English.MrsFileValidation"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NV.CT.NanoConsole.View.Control"
             xmlns:converter="clr-namespace:NV.CT.UI.Controls.Converter;assembly=NV.CT.UI.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

	<UserControl.Resources>
		<converter:FileValidationStateConverter x:Key="FileValidationStateConverter" />
	</UserControl.Resources>
	<Grid>
		<DataGrid
			Margin="0,10,0,10"
			HorizontalAlignment="Stretch"
			VerticalContentAlignment="Center"
			AutoGenerateColumns="False"
			BorderThickness="0"
			CanUserSortColumns="False"
			ColumnHeaderHeight="40"
			GridLinesVisibility="Horizontal"
			HorizontalScrollBarVisibility="Auto"
			IsReadOnly="True"
			ItemsSource="{Binding MrsFileValidationList}"
			RowHeight="40"
			SelectionUnit="Cell"
			VerticalScrollBarVisibility="Auto">
			<DataGrid.Columns>
				<DataGridTextColumn
					Width="6*"
					Binding="{Binding Name}"
					Header="Name" />
				<DataGridTemplateColumn
					Width="1*"
					Header="Status">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<Border>
								<Grid>
									<Image
										x:Name="imgState"
										Height="20"
										HorizontalAlignment="Left"
										VerticalAlignment="Center"
										Source="{Binding StatusString, Converter={StaticResource FileValidationStateConverter}}"/>
								</Grid>
							</Border>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>

			</DataGrid.Columns>
		</DataGrid>
	</Grid>

</UserControl>
