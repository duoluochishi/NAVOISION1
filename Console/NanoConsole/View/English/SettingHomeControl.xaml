<UserControl
	x:Class="NV.CT.NanoConsole.View.English.SettingHomeControl"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="clr-namespace:NV.CT.NanoConsole.View.English"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	d:DesignHeight="450"
	d:DesignWidth="800"
	Background="Black"
	mc:Ignorable="d">
	<Grid VerticalAlignment="Stretch">
		<Grid.RowDefinitions>
			<RowDefinition Height="50" />
			<RowDefinition Height="20" />
			<RowDefinition Height="100" />
			<RowDefinition Height="100" />
			<RowDefinition Height="100" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>
		<Label
			Grid.Row="0"
			Grid.Column="0"
			Grid.ColumnSpan="4"
			HorizontalAlignment="Center"
			VerticalAlignment="Center"
			HorizontalContentAlignment="Center"
			FontSize="14"
			Foreground="#05050F" />
		<ScrollViewer
			Grid.Row="2"
			Grid.RowSpan="4"
			Grid.Column="0"
			Grid.ColumnSpan="4"
			HorizontalAlignment="Center"
			VerticalAlignment="Top"
			HorizontalScrollBarVisibility="Disabled"
			VerticalScrollBarVisibility="Disabled">
			<ListBox
				Width="1680"
				Height="890"
				HorizontalAlignment="Center"
				VerticalAlignment="Center"
				Background="Transparent"
				BorderThickness="0"
				ItemsSource="{Binding SettingLinkListData}"
				Style="{x:Null}">
				<ListBox.ItemsPanel>
					<ItemsPanelTemplate>
						<WrapPanel
							Width="1660"
							Height="860"
							Margin="0,0,0,20"
							Background="Transparent"
							Orientation="Horizontal" />
					</ItemsPanelTemplate>
				</ListBox.ItemsPanel>
				<ListBox.ItemTemplate>
					<DataTemplate>
						<local:UCConfigBigMenuItem
							Title="{Binding ApplicationName}"
							Cursor="Hand"
							Description="{Binding Description}"
							Icon="{Binding IconGeometry}"
							IsEnabled="{Binding IsEnabled}">
							<local:UCConfigBigMenuItem.InputBindings>
								<MouseBinding
									Command="{Binding DataContext.Commands[OpenCommand], RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=ListBox}}"
									CommandParameter="{Binding CommandParameters}"
									MouseAction="LeftClick" />
							</local:UCConfigBigMenuItem.InputBindings>
						</local:UCConfigBigMenuItem>
					</DataTemplate>
				</ListBox.ItemTemplate>
			</ListBox>
		</ScrollViewer>
	</Grid>
</UserControl>
