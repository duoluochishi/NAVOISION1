<Window
    x:Class="NV.CT.PatientManagement.View.English.RawDataManagementWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converter="clr-namespace:NV.CT.UI.Controls.Converter;assembly=NV.CT.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:domain="clr-namespace:NV.CT.PatientManagement.Models"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:NV.CT.PatientManagement.View.English"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:res="clr-namespace:NV.CT.Language;assembly=NV.CT.Language"
    Title="RawDataManagementWindow"
    Width="800"
    Height="760"
    AllowsTransparency="True"
    Background="Transparent"
    ResizeMode="NoResize"
    WindowStyle="None"
    mc:Ignorable="d">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converter:DiskSourceStyleConverter x:Key="diskSourceStyleConverter" />
        <converter:ExportedStatusStyleConverter x:Key="exportedStatusStyleConverter" />
        <converter:FolderStyleConverter x:Key="folderStyleConverter" />
    </Window.Resources>
    <Border CornerRadius="10" Style="{StaticResource WindowsBorder}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20" />
                <ColumnDefinition />
                <ColumnDefinition Width="20" />
                <ColumnDefinition />
                <ColumnDefinition Width="20" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Border
                Grid.ColumnSpan="5"
                Background="#5A5A89"
                CornerRadius="10,10,0,0"
                Opacity="0.3" />
            <Grid Grid.ColumnSpan="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="40" />
                    <ColumnDefinition Width="30" />
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Button
                    Margin="10,5,0,4"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"
                    Opacity="0.7">
                    <Path
                        Width="15"
                        Height="15"
                        Fill="{StaticResource TextBoxBrush}"
                        Stretch="Fill"
                        Style="{StaticResource RawData}" />
                </Button>
                <TextBlock
                    Name="txtCaption"
                    Grid.Column="1"
                    Grid.ColumnSpan="2"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Text="{x:Static res:LanguageResource.Content_RawDataManagement}" />
                <Button
                    Grid.Column="3"
                    Command="{Binding Commands[CloseCommand]}"
                    CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
                    Style="{StaticResource TileCloseButtonStyle}"
                    ToolTip="{x:Static res:LanguageResource.Content_Close}">
                    <Path Style="{StaticResource TileClosePathStyle}" />
                </Button>
            </Grid>
            <Grid Grid.Row="1" Grid.ColumnSpan="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="10" />
                    <!--  0 Space  -->
                    <RowDefinition Height="28" />
                    <!--  1 Raw data list  -->
                    <RowDefinition Height="280" />
                    <!--  2 grid  -->
                    <RowDefinition Height="28" />
                    <!--  3 Target Area  -->
                    <RowDefinition Height="280" />
                    <!--  4 TreeView  -->
                    <RowDefinition Height="40" />
                    <!--  5 Chosen path  -->
                    <RowDefinition Height="40" />
                    <!--  6 buttons  -->
                    <RowDefinition Height="0" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <!--  Type  -->
                <Grid
                    Grid.Row="1"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Visibility="Hidden">
                    <TextBlock
                        x:Name="lblRawDataList"
                        Margin="10,0,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        FontSize="15"
                        Style="{StaticResource SectionTitleTextBlock}"
                        Text="{x:Static res:LanguageResource.Content_RawDataList}" />
                    <Rectangle
                        Width="{Binding ElementName=lblRawDataList, Path=ActualWidth}"
                        Margin="10,0,0,0"
                        Style="{StaticResource SectionTitleUnderlineRectangle}" />
                </Grid>
                <Grid
                    Grid.Row="2"
                    Grid.Column="0"
                    Grid.ColumnSpan="5"
                    Margin="0,0,0,0"
                    Visibility="Hidden">
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition Height="10" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <!--  生数据列表 DataGrid  -->
                    <DataGrid
                        x:Name="dgRawDataList"
                        Margin="0,10,0,0"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        ItemsSource="{Binding RawDataList}"
                        SelectedItem="{Binding SelectedRawData, Mode=TwoWay}"
                        SelectionMode="Extended"
                        Style="{StaticResource CommonDataGrid}">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Width="50">
                                <DataGridTemplateColumn.HeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock />
                                    </DataTemplate>
                                </DataGridTemplateColumn.HeaderTemplate>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn
                                Width="auto"
                                MinWidth="200"
                                MaxWidth="230"
                                Header="{x:Static res:LanguageResource.Header_ScanRange}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Style="{StaticResource DataGridContentTextBlock}" Text="{Binding ScanRange}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Width="180" Header="{x:Static res:LanguageResource.Header_CreatedTime}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Style="{StaticResource DataGridContentTextBlock}" Text="{Binding CreatedTime, StringFormat='yyyy/MM/dd HH:mm:ss'}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Width="180" Header="{x:Static res:LanguageResource.Header_ScanEndtime}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Style="{StaticResource DataGridContentTextBlock}" Text="{Binding ScanEndtime, StringFormat='yyyy/MM/dd HH:mm:ss'}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Width="120" Header="{x:Static res:LanguageResource.Header_ExportStatus}">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Path
                                            Width="22"
                                            Height="22"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Style="{Binding IsExported, Converter={StaticResource exportedStatusStyleConverter}}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
                <Grid
                    Grid.Row="1"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch">
                    <!--  Target Area  -->
                    <TextBlock
                        x:Name="lblTargetArea"
                        Margin="10,0,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        FontSize="15"
                        Style="{StaticResource SectionTitleTextBlock}"
                        Text="{x:Static res:LanguageResource.Content_TargetArea}" />
                    <Rectangle
                        Width="{Binding ElementName=lblTargetArea, Path=ActualWidth}"
                        Margin="10,0,0,0"
                        Style="{StaticResource SectionTitleUnderlineRectangle}" />
                </Grid>
                <Grid
                    Grid.Row="2"
                    Grid.RowSpan="3"
                    Grid.Column="0"
                    Grid.ColumnSpan="5"
                    Margin="0,0,0,10">
                    <Grid.RowDefinitions>
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <TreeView
                        x:Name="treeDriver"
                        Height="620"
                        MinWidth="620"
                        VerticalAlignment="Top"
                        BorderThickness="1,1,1,1"
                        ItemsSource="{Binding DriverTypes}"
                        SelectedValuePath="VituralPath">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="SelectedItemChanged">
                                <i:InvokeCommandAction Command="{Binding Commands[SelectedTreeItemChangedCommand]}" CommandParameter="{Binding ElementName=treeDriver, Path=SelectedItem}" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        <TreeView.Resources>
                            <Style BasedOn="{StaticResource MaterialDesignTreeViewItem}" TargetType="TreeViewItem">
                                <Setter Property="materialDesign:TreeViewAssist.ShowSelection" Value="True" />
                            </Style>
                            <HierarchicalDataTemplate DataType="{x:Type domain:DriverType}" ItemsSource="{Binding Folders}">
                                <StackPanel Orientation="Horizontal">
                                    <Path
                                        Width="15"
                                        Height="15"
                                        Margin="0,0,4,0"
                                        Fill="White"
                                        Stretch="Fill"
                                        Style="{Binding Path=DirectoryTreeItemType, Converter={StaticResource diskSourceStyleConverter}}" />
                                    <TextBlock
                                        Margin="2,2,0,2"
                                        Tag="{Binding FullPath}"
                                        Text="{Binding Name}" />
                                </StackPanel>
                            </HierarchicalDataTemplate>
                            <HierarchicalDataTemplate DataType="{x:Type domain:Folder}" ItemsSource="{Binding SubFolders}">
                                <StackPanel Orientation="Horizontal">
                                    <Path
                                        Width="15"
                                        Height="15"
                                        Margin="0,0,4,0"
                                        Fill="White"
                                        Stretch="Fill"
                                        Style="{Binding IsLogicalDisk, Converter={StaticResource folderStyleConverter}}" />
                                    <TextBlock
                                        Margin="0,2"
                                        Tag="{Binding FullPath}"
                                        Text="{Binding Name}" />
                                </StackPanel>
                            </HierarchicalDataTemplate>
                        </TreeView.Resources>
                        <TreeView.ItemContainerStyle>
                            <Style BasedOn="{StaticResource MaterialDesignTreeViewItem}" TargetType="TreeViewItem">
                                <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                                <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                            </Style>
                        </TreeView.ItemContainerStyle>
                        <TreeView.ContextMenu>
                            <ContextMenu Placement="MousePoint" Style="{StaticResource PatientManagementExportContextMenu}">
                                <MenuItem
                                    x:Name="menuItem_AddSubfolder"
                                    Height="50"
                                    Padding="10,5"
                                    Command="{Binding Commands[AddSubfolderCommand]}"
                                    Header="{x:Static res:LanguageResource.Content_AddSubfolder}"
                                    IsEnabled="{Binding IsMenuAddEnabled}">
                                    <MenuItem.Icon>
                                        <Path
                                            Fill="{StaticResource TextBoxBrush}"
                                            Stretch="Fill"
                                            Style="{StaticResource AddGeometry}" />
                                    </MenuItem.Icon>
                                </MenuItem>
                                <MenuItem
                                    x:Name="menuItem_EditFolderName"
                                    Height="50"
                                    Padding="10,5"
                                    Command="{Binding Commands[EditFolderNameCommand]}"
                                    Header="{x:Static res:LanguageResource.Content_RenameFolder}"
                                    IsEnabled="{Binding IsMenuEditEnabled}">
                                    <MenuItem.Icon>
                                        <Image Source="pack://application:,,,/NV.CT.UI.Controls;component/Icons/Custom.png" Stretch="Fill" />
                                    </MenuItem.Icon>
                                </MenuItem>
                                <MenuItem
                                    x:Name="menuItem_DeleteFolder"
                                    Height="50"
                                    Padding="10,5"
                                    Command="{Binding Commands[DeleteFolderCommand]}"
                                    Header="{x:Static res:LanguageResource.Content_DeleteFolder}"
                                    IsEnabled="{Binding IsMenuEditEnabled}">
                                    <MenuItem.Icon>
                                        <Path
                                            Fill="{StaticResource TextBoxBrush}"
                                            Stretch="Fill"
                                            Style="{StaticResource Delete}" />
                                    </MenuItem.Icon>
                                </MenuItem>
                            </ContextMenu>
                        </TreeView.ContextMenu>
                    </TreeView>
                </Grid>
                <TextBlock
                    Grid.Row="5"
                    Grid.Column="0"
                    Grid.ColumnSpan="5"
                    Margin="10,0,10,10"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    FontSize="12"
                    Foreground="#FFA63F"
                    Text="{Binding OmittedChosenPath}"
                    ToolTip="{Binding ElementName=treeDriver, Path=SelectedValue}" />
                <!--  底部 导出和关闭 按钮 Begin  -->
                <TextBlock
                    Grid.Row="6"
                    Grid.Column="0"
                    Grid.ColumnSpan="3"
                    Width="420"
                    Margin="10,0,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    FontSize="12"
                    Foreground="#FFA63F"
                    Text="{Binding MessageContent}" />
                <StackPanel
                    Grid.Row="6"
                    Grid.Column="3"
                    Grid.ColumnSpan="2"
                    Margin="0,0,40,0"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
                    <Button
                        x:Name="btnExport"
                        Width="150"
                        Height="40"
                        Margin="0,0,20,0"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        Content="{x:Static res:LanguageResource.Content_Export}"
                        IsEnabled="{Binding IsExportButtonEnabled}">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Click">
                                <i:InvokeCommandAction Command="{Binding Commands[ExportCommand]}"  CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </Button>
                    <Button
                        x:Name="btnClose"
                        Width="150"
                        Height="40"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        Content="{x:Static res:LanguageResource.Content_Cancel}">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Click">
                                <i:InvokeCommandAction Command="{Binding Commands[CloseCommand]}" CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </Button>
                </StackPanel>
                <!--  底部 导出和关闭 按钮 End  -->
            </Grid>
        </Grid>
    </Border>
</Window>
