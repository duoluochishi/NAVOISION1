<Window x:Class="NV.CT.PatientManagement.View.English.RawDataImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NV.CT.PatientManagement.View.English"
        xmlns:res="clr-namespace:NV.CT.Language;assembly=NV.CT.Language"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="RawDataImportWindow" 
        Width="800" Height="230" Background="Transparent"
        ResizeMode="NoResize" AllowsTransparency="True" WindowStyle="None" >
    <Border CornerRadius="10" Style="{StaticResource WindowsBorder}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20" />
                <ColumnDefinition />
                <ColumnDefinition Width="20" />
                <ColumnDefinition />
                <ColumnDefinition Width="20" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="auto" />
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Border Grid.ColumnSpan="5" Background="#5A5A89" CornerRadius="10,10,0,0" Opacity="0.3" />
            <Grid Grid.ColumnSpan="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="40" />
                    <ColumnDefinition Width="30" />
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Button Margin="10,5,0,4" HorizontalAlignment="Left" VerticalAlignment="Center" Background="Transparent" BorderThickness="0" Cursor="Hand" Opacity="0.7">
                    <Path Style="{StaticResource ImportRawData}" Width="17" Height="17" Fill="{StaticResource TextBoxBrush}" Stretch="Fill"/>
                </Button>
                <TextBlock Name="txtCaption" Grid.Column="1" Grid.ColumnSpan="2" Text="{x:Static res:LanguageResource.Content_ImportRawData}"
                           Width="430" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                <Button Grid.Column="3" Style="{StaticResource TileCloseButtonStyle}" ToolTip="{x:Static res:LanguageResource.Content_Close}"
                        Command="{Binding  Commands[CloseCommand]}" 
                        CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}">
                    <Path Style="{StaticResource TileClosePathStyle}" />
                </Button>
            </Grid>
            <Grid  Grid.Row="1" Grid.ColumnSpan="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="10" />
                    <!--space-->
                    <RowDefinition Height="28" />
                    <RowDefinition Height="80" />
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="40"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <TextBlock x:Name="lblSourcePath" Grid.Row="0" Margin="10,0,0,0" Text="{x:Static res:LanguageResource.Content_SourcePath}"
                               HorizontalAlignment="Left" VerticalAlignment="Top" FontSize="14" Style="{StaticResource SectionTitleTextBlock}" />
                    <Rectangle Width="{Binding ElementName=lblSourcePath,Path=ActualWidth}"
                               Style="{StaticResource SectionTitleUnderlineRectangle}" Margin="10,0,0,0"/>
                </Grid>
                <Grid Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="5" Margin="0,0,10,0">
                    <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                        <Button x:Name="btnSelectPath" Content="{x:Static res:LanguageResource.Content_SelectPath}"
                            Width="100" Height="34" Margin="10,0,0,0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <i:InvokeCommandAction Command="{Binding Commands[SelectPathCommand]}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>
                        <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,10,10,10" FontSize="12" Foreground="#FFA63F"
                                   Text="{Binding OmittedChosenPath}" ToolTip="{Binding ChosenPath}"/>
                    </StackPanel>
                </Grid>
                <!--  底部 导出和关闭 按钮 Begin -->
                <TextBlock Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3" Width="420"
                           HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0,0,0"
                           Text="{Binding MessageContent}" FontSize="12" Foreground="#FFA63F"/>
                <StackPanel Grid.Row="4" Grid.Column="3" Grid.ColumnSpan="2" HorizontalAlignment="Right" Orientation="Horizontal" Margin="0,0,20,0">
                    <Button x:Name="btnImport"  IsEnabled="{Binding IsImportButtonEnabled}" Content="{x:Static res:LanguageResource.Content_Import}"
                            Width="150" Height="40" Margin="0,0,20,0"
                            HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Click">
                                <i:InvokeCommandAction Command="{Binding Commands[ImportCommand]}"  CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"/>
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </Button>
                    <Button x:Name="btnClose" Content="{x:Static res:LanguageResource.Content_Cancel}"
                            Width="150" Height="40" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Click">
                                <i:InvokeCommandAction
                                   Command="{Binding Commands[CloseCommand]}"
                                   CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </Button>
                </StackPanel>
                <!--  底部 导出和关闭 按钮 End -->
            </Grid>
        </Grid>
    </Border>
</Window>
