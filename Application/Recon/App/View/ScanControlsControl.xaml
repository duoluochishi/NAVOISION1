<UserControl
	x:Class="NV.CT.Recon.View.ScanControlsControl"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:res="clr-namespace:NV.CT.Language;assembly=NV.CT.Language"
	d:DesignHeight="240"
	d:DesignWidth="540"
	mc:Ignorable="d">
	<UserControl.Resources>
		<BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
		<Style
			x:Key="ScanControlsStyle"
			BasedOn="{StaticResource MaterialDesignRaisedButton}"
			TargetType="{x:Type Button}">
			<Setter Property="FontSize" Value="15px" />
			<Setter Property="Foreground" Value="{StaticResource BaseForegroundColor}" />
			<Setter Property="VerticalAlignment" Value="Center" />
			<Setter Property="VerticalContentAlignment" Value="Center" />
			<Setter Property="Background" Value="{StaticResource ButtonMainBackground}" />
			<Setter Property="Height" Value="42" />
			<Style.Triggers>
				<Trigger Property="IsEnabled" Value="False">
					<Setter Property="Background" Value="#5a5a89" />
				</Trigger>
				<Trigger Property="IsEnabled" Value="True">
					<Setter Property="Background" Value="#1665D8" />
				</Trigger>
			</Style.Triggers>
		</Style>
	</UserControl.Resources>
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="90" />
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="90" />
		</Grid.ColumnDefinitions>


		<Button
			Grid.Column="1"
			Command="{Binding Commands[ReconAllCommand]}"
			Content="Recon All"
			IsEnabled="{Binding IsReconAllEnable, Mode=TwoWay}"
			Style="{StaticResource ScanControlsStyle}" />

		<!--<Button
            Grid.Column="3"
            Command="{Binding Commands[GoCommand]}"
            Content="{x:Static res:LanguageResource.Content_Go}"
            IsEnabled="{Binding IsGoEnable, Mode=TwoWay}"
            Visibility="{Binding IsGoVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Button.Style>
                <Style
                    BasedOn="{StaticResource MaterialDesignFloatingActionMiniDarkButton}"
                    TargetType="{x:Type Button}">
                    <Setter Property="Width" Value="60" />
                    <Setter Property="Height" Value="60" />
                    <Setter Property="FontSize" Value="15px" />
                    <Style.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" Value="#5a5a89" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="True">
                            <Setter Property="Background" Value="#1665D8" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

        <Button
            Grid.Column="3"
            Command="{Binding Commands[CloseCommand]}"
            Content="{x:Static res:LanguageResource.Content_Close}"
            IsEnabled="{Binding IsCloseEnable, Mode=TwoWay}"
            Visibility="{Binding IsCloseVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Button.Style>
                <Style
                    BasedOn="{StaticResource MaterialDesignFloatingActionMiniDarkButton}"
                    TargetType="{x:Type Button}">
                    <Setter Property="Width" Value="60" />
                    <Setter Property="Height" Value="60" />
                    <Setter Property="FontSize" Value="15px" />
                    <Style.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" Value="#5a5a89" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="True">
                            <Setter Property="Background" Value="#1665D8" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>-->

		<!--<Button
			Grid.Column="5"
			Command="{Binding Commands[CancelCommand]}"
			Content="{x:Static res:LanguageResource.Content_Cancel}"
			IsEnabled="{Binding IsCancelEnable, Mode=TwoWay}"
			Style="{StaticResource ScanControlsStyle}" />-->

		<!--Cancel功能  暂时隐藏这个功能-->
		<!--<Button
			Grid.Column="5"
			Command="{Binding Commands[CancelCommand]}"
			Content="{x:Static res:LanguageResource.Content_Cancel}"
			IsEnabled="False"
			Style="{StaticResource ScanControlsStyle}" />-->

		<!--<Button
			Grid.Column="5"
			Command="{Binding Commands[CloseCommand]}"
			Content="{x:Static res:LanguageResource.Content_Close}"
			Style="{StaticResource ScanControlsStyle}" />-->

	</Grid>
</UserControl>