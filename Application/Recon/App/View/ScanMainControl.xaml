<UserControl
	x:Class="NV.CT.Recon.View.ScanMainControl"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:res="clr-namespace:NV.CT.Language;assembly=NV.CT.Language"
	xmlns:view="clr-namespace:NV.CT.UI.Exam.View;assembly=NV.CT.UI.Exam"
	xmlns:view1="clr-namespace:NV.CT.Recon.View"
	d:DesignHeight="950"
	d:DesignWidth="1950"
	mc:Ignorable="d">
	<UserControl.Resources>
		<Style
			x:Key="BaseLiftMargin"
			TargetType="{x:Type Control}">
			<Setter Property="Margin" Value="5,0,0,0" />
		</Style>
		<Style
			x:Key="BaseRightStackPanel"
			TargetType="{x:Type StackPanel}">
			<Setter Property="Orientation" Value="Vertical" />
			<Setter Property="HorizontalAlignment" Value="Stretch" />
			<Setter Property="VerticalAlignment" Value="Stretch" />
			<Style.Triggers>
				<Trigger Property="IsEnabled" Value="True">
					<Setter Property="Visibility" Value="Visible" />
				</Trigger>
				<Trigger Property="IsEnabled" Value="False">
					<Setter Property="Visibility" Value="Collapsed" />
				</Trigger>
			</Style.Triggers>
		</Style>
		<Style
			x:Key="BorderStyle"
			TargetType="{x:Type Border}">
			<Style.Triggers>
				<Trigger Property="IsEnabled" Value="True">
					<Setter Property="Visibility" Value="Visible" />
				</Trigger>
				<Trigger Property="IsEnabled" Value="False">
					<Setter Property="Visibility" Value="Collapsed" />
				</Trigger>
			</Style.Triggers>
		</Style>
	</UserControl.Resources>
	<AdornerDecorator>
		<Grid Background="#05050F">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="340" />
				<ColumnDefinition Width="0" />
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="5" />
			</Grid.ColumnDefinitions>
			<Border
				Grid.Column="0"
				CornerRadius="10"
				Style="{StaticResource WindowsBorder}">
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="*" />
						<RowDefinition Height="200" />
						<RowDefinition Height="100" />
						<RowDefinition Height="100" />
					</Grid.RowDefinitions>
					<view1:TaskListControl Grid.Row="0" />
					<view1:ScanControlsControl Grid.Row="3" />
				</Grid>
			</Border>

			<Border Grid.Column="2">
				<ContentControl
					x:Name="LayoutContainer"
					HorizontalContentAlignment="Stretch"
					VerticalContentAlignment="Stretch" />
			</Border>

		</Grid>
	</AdornerDecorator>
</UserControl>