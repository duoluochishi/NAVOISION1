<UserControl
    x:Class="NV.CT.UI.Exam.DynamicParameters.Parameters.Scan.DLP"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"   
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"   
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"   
    xmlns:res="clr-namespace:NV.CT.Language;assembly=NV.CT.Language"
    d:DesignHeight="80" d:DesignWidth="800" mc:Ignorable="d">
    <!--  DoseLengthProduct  -->
    <UserControl.Resources>
        <Style x:Key="DLPTextBlockStyle" BasedOn="{StaticResource MaterialDesignTextBlock}" TargetType="{x:Type TextBlock}">
            <Setter Property="FontSize" Value="14px" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource SExposuresIconFillBrush}" />          
        </Style>
        <Style x:Key="DLPsTextBlockStyle" BasedOn="{StaticResource MaterialDesignTextBlock}" TargetType="{x:Type TextBlock}">
            <Setter Property="FontSize" Value="14px" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{StaticResource SExposuresIconFillBrush}" />           
        </Style>
        <Style x:Key="ImageStyle"  TargetType="{x:Type Image}">
            <Setter Property="Width" Value="20" />
            <Setter Property="Height" Value="20" />
            <Setter Property="Margin" Value="-55 0 0 0" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="True">
                    <Setter Property="Visibility" Value="Collapsed" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Visibility" Value="Visible" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="LabelStyle" BasedOn="{StaticResource NanoLabelStyle}" TargetType="{x:Type Label}">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="25*" />
            <ColumnDefinition Width="40*" />
            <ColumnDefinition Width="20*" />
            <ColumnDefinition Width="10*" />
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" VerticalAlignment="Center"  Content="{x:Static res:LanguageResource.Content_DLP}" />
        <Grid  Grid.Column="1" HorizontalAlignment="Stretch">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="45*" />
                <ColumnDefinition Width="10*" />
                <ColumnDefinition Width="45*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Style="{StaticResource DLPTextBlockStyle}" Text="{Binding Path=DLP, Mode=TwoWay}" />
            <Label Grid.Column="1"  Content="/"  Style="{StaticResource LabelStyle}"/>
            <TextBlock Grid.Column="2" Style="{StaticResource DLPsTextBlockStyle}" Text="{Binding Path=DLPs, Mode=TwoWay}" />
        </Grid>
        <Label Grid.Column="2" VerticalAlignment="Center" Content="{x:Static res:LanguageResource.Content_mGycm}" />
        <Image Grid.Column="3" IsEnabled="{Binding IsDLPEnabled}" Style="{StaticResource ImageStyle}" Source="{Binding DLPImage}"/>
    </Grid>
</UserControl>