<UserControl
    x:Class="NV.CT.UI.Exam.View.MessageTooltipControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="100"
    d:DesignWidth="340"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource SExposuresIconFillBrush}" />
        </Style>
        <Style
            x:Key="ImageStyle"
            TargetType="Image">
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="True">
                    <Setter Property="Visibility" Value="Visible" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Visibility" Value="Collapsed" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="StackPanelStyle" TargetType="StackPanel">
            <Setter Property="Height" Value="46" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Orientation" Value="Horizontal" />
        </Style>
        <Style x:Key="TextBlockStyle" BasedOn="{StaticResource NanoTextBlockStyle}" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Foreground" Value="{StaticResource SExposuresIconFillBrush}" />
        </Style>
    </UserControl.Resources>
    <StackPanel Style="{StaticResource StackPanelStyle}">
        <Image
            IsEnabled="{Binding Path=IsScanning, Mode=TwoWay}"
            RenderTransformOrigin="0.5,0.5"
            Source="pack://application:,,,/NV.CT.UI.Controls;component/Images/Scanning.png"
            Style="{StaticResource ImageStyle}">
            <Image.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard
                            RepeatBehavior="Forever"
                            SpeedRatio="2"
                            Timeline.DesiredFrameRate="24">
                            <DoubleAnimation
                                RepeatBehavior="Forever"
                                Storyboard.TargetProperty="(Image.RenderTransform).(RotateTransform.Angle)"
                                From="0"
                                To="360"
                                Duration="0:0:4" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Image.Triggers>
            <Image.RenderTransform>
                <RotateTransform Angle="0" CenterX="0.5" CenterY="0.4" />
            </Image.RenderTransform>
        </Image>
        <Image
            IsEnabled="{Binding Path=IsBeforeScan, Mode=TwoWay}"
            Source="pack://application:,,,/NV.CT.UI.Controls;component/Images/Beforescan.png"
            Style="{StaticResource ImageStyle}" />
        <Image
            IsEnabled="{Binding Path=IsMoveTable, Mode=TwoWay}"
            Source="pack://application:,,,/NV.CT.UI.Controls;component/Images/Movetable.png"
            Style="{StaticResource ImageStyle}" />
        <TextBlock Style="{StaticResource TextBlockStyle}" Text="{Binding Path=ScanMessage, Mode=TwoWay}" />
    </StackPanel>
</UserControl>