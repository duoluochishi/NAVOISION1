<Window x:Class="NV.CT.UI.Exam.View.PostProcessSettingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:res="clr-namespace:NV.CT.Language;assembly=NV.CT.Language"
        xmlns:wpf="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:local="clr-namespace:NV.CT.UI.Exam.DynamicParameters.Parameters.Recon"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Height="760" Width="800" Background="Transparent" ResizeMode="NoResize">
    <Window.Resources>
        <!-- post process -->
        <SolidColorBrush x:Key="ButtonBackColor" Color="#5a5a89" Opacity="0.28"/>
        <Style x:Key="BorderStyle" TargetType="{x:Type Border}">
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="True">
                    <Setter Property="Visibility" Value="Visible" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Visibility" Value="Collapsed" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StackPanelGrid" TargetType="{x:Type Grid}">
            <Setter Property="Width" Value="500" />
            <Setter Property="Margin" Value="15,5,0,5" />
        </Style>
        <Style x:Key="ButtonStyle" TargetType="wpf:PackIcon">
            <Setter Property="Width" Value="25" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Height" Value="25" />
            <Setter Property="Foreground" Value="#90EE90" />
        </Style>
        <Style x:Key="ConfirmButtonStyle" TargetType="Button" BasedOn="{StaticResource  MaterialDesignRaisedButton}">
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="37"/>
            <Setter Property="Margin" Value="25 0 120 15"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Content" Value="{x:Static res:LanguageResource.Content_Confirm}"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#5a5a89" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="True">
                    <Setter Property="Background" Value="#1665D8" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource  MaterialDesignRaisedButton}">
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="37"/>
            <Setter Property="Margin" Value="15 0 25 15"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Content" Value="{x:Static res:LanguageResource.Content_Close}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="{StaticResource ButtonBackColor}"/>
        </Style>
        <Style x:Key="TitleBorderStyle" TargetType="{x:Type Border}">
            <Setter Property="Opacity" Value="0.3"/>
            <Setter Property="Background" Value="{StaticResource BaseTabBackBrush}"/>
            <Setter Property="CornerRadius" Value="5 5 0 0" />
        </Style>
        <Style x:Key="TilePathStyle" BasedOn="{StaticResource List01}" TargetType="{x:Type Path}">
            <Setter Property="Width" Value="20" />
            <Setter Property="Height" Value="20" />
            <Setter Property="Fill" Value="{StaticResource TextBoxBrush}" />
            <Setter Property="Stretch" Value="Fill" />
            <Setter Property="Margin" Value="10,6,10,6" />
        </Style>
        <Style x:Key="XButtonStyle" BasedOn="{StaticResource MaterialDesignIconButton}" TargetType="Button">
            <Setter Property="Width" Value="22" />
            <Setter Property="Margin" Value="0,0,25,0" />
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </Window.Resources>

    <Border CornerRadius="10" Style="{StaticResource WindowsBorder}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36" />
                <RowDefinition Height="*" />
                <RowDefinition Height="90" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40" />
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="80" />
            </Grid.ColumnDefinitions>

            <Border Grid.Row="0"  Grid.Column="0" Grid.ColumnSpan="4" Style="{StaticResource TitleBorderStyle}"/>
            <Path Style="{StaticResource TilePathStyle}" Grid.Row="0" Grid.Column="0" />
            <Label Grid.Row="0" Grid.Column="1" Content="{x:Static res:LanguageResource.Content_PostProcessSetting}" FontSize="16" Style="{StaticResource TitleLabelStyle}" />
            <Button  Style="{StaticResource XButtonStyle}" Grid.Row="0" Grid.Column="3"         
                Command="{Binding Commands[CloseCommand]}" CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}">
                <Path Style="{StaticResource TileClosePathStyle}" />
            </Button>
            <Grid Grid.Row="1" Grid.Column="0" Margin ="0 5 0 0" Grid.ColumnSpan="4">
                <StackPanel Orientation="Vertical">
                    <Grid Style="{StaticResource StackPanelGrid}">
                        <local:PostProcessPostDenoise />
                    </Grid>
                    <Grid Style="{StaticResource StackPanelGrid}">
                        <local:PostProcessSharp />
                    </Grid>
                    <Grid Style="{StaticResource StackPanelGrid}" Visibility="Collapsed">
                        <local:PitchArtifactReduce />
                    </Grid>
                    <Grid Style="{StaticResource StackPanelGrid}" >
                        <local:MotionArtifactReduce />
                    </Grid>
                    <Grid Style="{StaticResource StackPanelGrid}">
                        <local:ConeAngleArtifactReduce />
                    </Grid>
                    <Grid Style="{StaticResource StackPanelGrid}" Visibility="Collapsed">
                        <local:WindmillArtifactReduce />
                    </Grid>
                    <Grid Style="{StaticResource StackPanelGrid}" Visibility="Collapsed">
                        <local:StreakArtifactReduce />
                    </Grid>
                    <Grid Style="{StaticResource StackPanelGrid}" Visibility="Collapsed">
                        <local:SparseArtifactReduce10 />
                    </Grid>
                    <Grid Style="{StaticResource StackPanelGrid}" Visibility="Collapsed">
                        <local:SparseArtifactReduce20 />
                    </Grid>
                    <Grid Style="{StaticResource StackPanelGrid}">
                        <local:SkullArtifactReduce />
                    </Grid>

                    <Grid VerticalAlignment="Center">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150" />
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="1" Content="{x:Static res:LanguageResource.Content_PostProcessList}" Margin="10 5 0 5"/>
                    </Grid>
                    <ScrollViewer>
                        <Grid Style="{StaticResource StackPanelGrid}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="360" />
                            </Grid.RowDefinitions>
                            <DataGrid x:Name="postProcessList" Margin="5" Height="360" ItemsSource="{Binding SelectedPostProcessItems}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}" AutoGenerateColumns="False" CanUserAddRows="False" SelectionUnit="FullRow" SelectionMode="Single">
                                <DataGrid.Columns>
                                    <DataGridTemplateColumn Width="90" Header="{x:Static res:LanguageResource.Content_Remove}">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button  HorizontalAlignment="Center" Background="Transparent" BorderBrush="Transparent" Margin="0 0 0 0" Focusable="False">
                                                    <wpf:PackIcon Kind="MinusCircle" Style="{StaticResource ButtonStyle}" Foreground="#FF0000"/>
                                                    <i:Interaction.Triggers>
                                                        <i:EventTrigger EventName="PreviewMouseLeftButtonDown">
                                                            <i:InvokeCommandAction Command="{Binding DataContext.Commands[SelectCommand], RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" />
                                                        </i:EventTrigger>
                                                        <i:EventTrigger EventName="PreviewMouseLeftButtonUp">
                                                            <i:InvokeCommandAction Command="{Binding DataContext.Commands[RemoveCommand], RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding Key}" />
                                                        </i:EventTrigger>
                                                    </i:Interaction.Triggers>
                                                </Button>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <!--  method  -->
                                    <DataGridTemplateColumn Width="210" Header="{x:Static res:LanguageResource.Content_Method}">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Key}" HorizontalAlignment="Left" Style="{StaticResource DataGridContentTextBlock}" />
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <!--  parameters  -->
                                    <DataGridTemplateColumn Width="200" Header="{x:Static res:LanguageResource.Content_Parameters}">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Value}" Style="{StaticResource DataGridContentTextBlock}"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                            <Border Grid.Row="1" BorderBrush="#5A5A89" BorderThickness="1.4" CornerRadius="10" Opacity="1" />
                        </Grid>
                    </ScrollViewer>
                </StackPanel>
            </Grid>
            <Grid Grid.Row="2" VerticalAlignment="Bottom" Margin="0,0,0,15" Grid.Column="2" Grid.ColumnSpan="2">
                <Button Style="{StaticResource ConfirmButtonStyle}" IsEnabled="{Binding Path=IsConfirmButtonEnable}" Command="{Binding Commands[ConfirmPostProcessSettingCommand]}" CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" />
                <Button Style="{StaticResource CloseButtonStyle}" Command="{Binding Commands[CloseCommand]}" CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" />
            </Grid>
        </Grid>
    </Border>
</Window>
