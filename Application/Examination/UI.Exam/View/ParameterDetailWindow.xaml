<Window
    x:Class="NV.CT.UI.Exam.View.ParameterDetailWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"   
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"   
    Width="1000" Height="700"
    Background="Transparent" AllowsTransparency="True" ResizeMode="NoResize" WindowStyle="None" mc:Ignorable="d" x:Uid="WParameterDetailWindow" x:Name="WParameterDetailWindow">
    <Window.Resources>
        <Style x:Key="BorderStyle" TargetType="{x:Type Border}">
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="True">
                    <Setter Property="Visibility" Value="Visible" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Visibility" Value="Collapsed" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ContentControlStyle"
            TargetType="{x:Type ContentControl}">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
        </Style>
        <Style x:Key="TilePathStyle" BasedOn="{StaticResource List01}" TargetType="{x:Type Path}">
            <Setter Property="Width" Value="20" />
            <Setter Property="Height" Value="20" />
            <Setter Property="Fill" Value="{StaticResource TextBoxBrush}" />
            <Setter Property="Stretch" Value="Fill" />
            <Setter Property="Margin" Value="10,6,10,6" />
        </Style>
        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignIconButton}">
            <Setter Property="Width" Value="22"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    <Border CornerRadius="10" Style="{StaticResource WindowsBorder}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40" />
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="777*" />
            </Grid.ColumnDefinitions>
            <Border
                Grid.Row="0"
                Grid.Column="0"
                Grid.ColumnSpan="3"
                Background="{StaticResource BaseTabBackBrush}"
                CornerRadius="5 5 0 0"
                Opacity="0.3">
            </Border>
            <Path Style="{StaticResource TilePathStyle}" 
                Grid.Row="0"
                Grid.Column="0" />
            <Label
                Grid.Row="0"
                Grid.Column="1"
                Grid.ColumnSpan="2"
                Content="{Binding DetailTitle}"
                FontSize="16"
                Style="{StaticResource TitleLabelStyle}" />
            <Border
                 Grid.Column="3"
                 IsEnabled="{Binding IsReconAdvancedButtonShow}">
                <Border.Style>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="Margin" Value="5,0,45,0"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="HorizontalAlignment" Value="Right"/>
                        <Setter Property="Background" Value="Green"/>
                        <Setter Property="CornerRadius" Value="5"/>
                        <Style.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <CheckBox                    
                     Margin="5,0,5,0"
                     VerticalAlignment="Stretch"
                     Content="Advanced parameters"
                     FontSize="14"
                     IsChecked="{Binding IsReconAdvancedChecked, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" />
            </Border>

            <Button  Style="{StaticResource CloseButtonStyle}"
                Grid.Column="3"
                Command="{Binding Commands[CloseCommand]}"
                CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"  >
                <Path Style="{StaticResource TileClosePathStyle}" />
            </Button>

            <Border
                Grid.Row="1"
                Grid.Column="0"
                Grid.ColumnSpan="3"
                IsEnabled="{Binding Path=ScanDetailShow, Mode=TwoWay}"
                Style="{StaticResource BorderStyle}">
                <ContentControl Style="{StaticResource ContentControlStyle}"
                    x:Name="ScanParameterDetailContentHolder" />
            </Border>
            <Border
                Grid.Row="1"
                Grid.Column="0"
                Grid.ColumnSpan="3"
                IsEnabled="{Binding Path=ReconDetailShow, Mode=TwoWay}"
                Style="{StaticResource BorderStyle}">
                <ContentControl Style="{StaticResource ContentControlStyle}"
                    x:Name="ReconParameterDetailContentHolder" />
            </Border>
        </Grid>
    </Border>
</Window>