<Window
    x:Class="NV.CT.UI.Exam.View.TimeDensityWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"   
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"   
    xmlns:res="clr-namespace:NV.CT.Language;assembly=NV.CT.Language" 
    xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    Width="700" Height="420" Background="Transparent" AllowsTransparency="True" ResizeMode="NoResize"
    WindowStyle="None"  mc:Ignorable="d" Top="550" Left="770" WindowStartupLocation="Manual">
    <Window.Resources>       
        <Style x:Key="BorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#5a5a89"/>
            <Setter Property="Opacity" Value="0.3"/>
            <Setter Property="CornerRadius" Value="5 5 0 0"/>
        </Style>
        <Style x:Key="TilePathStyle" TargetType="{x:Type Path}" BasedOn="{StaticResource VoicePath}">
            <Setter Property="Margin" Value="5 0 0 0"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Fill" Value="White"/>
            <Setter Property="Height" Value="18"/>
            <Setter Property="Width" Value="20"/>
            <Setter Property="Stretch" Value="Fill" />
        </Style>
        <Style x:Key="TileTextBlockStyle" TargetType="TextBlock" BasedOn="{StaticResource  MaterialDesignTextBlock}">
            <Setter Property="Margin" Value="35 0 0 0"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="16px"/>
            <Setter Property="Foreground" Value="{StaticResource BaseForegroundColor}" />
        </Style>
        <Style x:Key="TimeDensityButtonStyle" TargetType="materialDesign:PackIcon">
            <Setter Property="Width" Value="25" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Height" Value="25" />
            <Setter Property="Foreground" Value="#90EE90" />
        </Style>
    </Window.Resources>
    <Border CornerRadius="10" Style="{StaticResource WindowsBorder}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36" />
                <RowDefinition Height="1*" />
            </Grid.RowDefinitions>
            <Border Style="{StaticResource BorderStyle}" />
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition Width="45"/>
                </Grid.ColumnDefinitions>
                <Path Grid.Column="0" Style="{StaticResource TilePathStyle}"/>
                <TextBlock Grid.Column="0" Grid.ColumnSpan="2" Style="{StaticResource TileTextBlockStyle}" Text="{x:Static res:LanguageResource.Text_TimeDensityCurve}" />
                <Button Grid.Column="2" Style="{StaticResource TileCloseButtonStyle}" Command="{Binding  Commands[CloseCommand]}" CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}">
                    <Path Style="{StaticResource TileClosePathStyle}" />
                </Button>
            </Grid>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="45"/>
                </Grid.ColumnDefinitions>
                <lvc:CartesianChart Grid.Column="0" AnimationsSpeed="0" Series="{Binding Series}" LegendPosition="Top" LegendTextPaint="{Binding LegendTextPaintB}" XAxes="{Binding XAxis}" YAxes="{Binding YAxis}" TooltipPosition="Hidden"></lvc:CartesianChart>
                <Line X1="10" Y1="342" X2="650" Y2="342" Stroke="#1976D2" StrokeThickness="1" Grid.Column="0" />
                
                <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center">
                    <Button Command="{Binding Commands[AddCommand]}" Background="Transparent" BorderBrush="Transparent">
                        <materialDesign:PackIcon Kind="PlusCircle" Style="{StaticResource TimeDensityButtonStyle}"/>
                    </Button>
                    <Button Command="{Binding Commands[RemoveCommand]}" Background="Transparent" BorderBrush="Transparent" Margin="0 10 0 0">
                        <materialDesign:PackIcon Kind="MinusCircle" Style="{StaticResource TimeDensityButtonStyle}" Foreground="#FF0000"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>