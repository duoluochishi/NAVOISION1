<Window
	x:Class="NV.CT.UI.Exam.View.DoorAlertWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="clr-namespace:NV.CT.UI.Exam.View"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:res="clr-namespace:NV.CT.Language;assembly=NV.CT.Language"
	xmlns:wpf="http://materialdesigninxaml.net/winfx/xaml/themes"
	Title="DoorAlertWindow"
	Width="450"
	Height="220"
	AllowsTransparency="True"
	Background="Transparent"
	ResizeMode="NoResize"
	WindowStyle="None"
	mc:Ignorable="d">
	<Window.Resources>
		<Style
			x:Key="BorderStyle"
			TargetType="{x:Type Border}">
			<Style.Triggers>
				<Trigger Property="IsEnabled" Value="True">
					<Setter Property="Visibility" Value="Visible" />
				</Trigger>
				<Trigger Property="IsEnabled" Value="False">
					<Setter Property="Visibility" Value="Collapsed" />
				</Trigger>
			</Style.Triggers>
		</Style>
		<Style
			x:Key="TilePathStyle"
			BasedOn="{StaticResource List01}"
			TargetType="{x:Type Path}">
			<Setter Property="Width" Value="20" />
			<Setter Property="Height" Value="20" />
			<Setter Property="Fill" Value="{StaticResource TextBoxBrush}" />
			<Setter Property="Stretch" Value="Fill" />
			<Setter Property="Margin" Value="10,6,10,6" />
		</Style>
		<Style
			x:Key="CloseButtonStyle"
			BasedOn="{StaticResource MaterialDesignIconButton}"
			TargetType="Button">
			<Setter Property="Width" Value="22" />
			<Setter Property="Margin" Value="255,0,0,0" />
			<Setter Property="HorizontalAlignment" Value="Left" />
			<Setter Property="VerticalAlignment" Value="Center" />
		</Style>
		<Style
			x:Key="CancelButtonStyle"
			BasedOn="{StaticResource MaterialDesignRaisedButton}"
			TargetType="{x:Type Button}">
			<Setter Property="Height" Value="40" />
			<Setter Property="Width" Value="100" />
			<Setter Property="HorizontalAlignment" Value="Center" />
			<Setter Property="Background" Value="{StaticResource BaseBackgroundColor}" />
			<Setter Property="wpf:ButtonAssist.IsEnable" Value="{Binding RelativeSource={RelativeSource Mode=Self}, Path=IsEnabled, Mode=TwoWay}" />
		</Style>
		<Style
			x:Key="MessageImageStyle"
			TargetType="Image">
			<Setter Property="Margin" Value="0,0,0,23" />
			<Setter Property="Height" Value="48" />
			<Setter Property="HorizontalAlignment" Value="Center" />
			<Setter Property="VerticalAlignment" Value="Center" />
			<Setter Property="Opacity" Value="0.9" />
		</Style>
	</Window.Resources>
	<Border
		CornerRadius="10"
		Style="{StaticResource WindowsBorder}">
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="32" />
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="40" />
				<ColumnDefinition Width="80" />
				<ColumnDefinition Width="103*" />
				<ColumnDefinition Width="777*" />
			</Grid.ColumnDefinitions>
			<Border
				Grid.Row="0"
				Grid.Column="0"
				Grid.ColumnSpan="4"
				Background="{StaticResource BaseTabBackBrush}"
				CornerRadius="5 5 0 0"
				Opacity="0.3" />
			<Path
				Grid.Row="0"
				Grid.Column="0"
				Style="{StaticResource TilePathStyle}" />
			<Label
				Grid.Row="0"
				Grid.Column="1"
				Grid.ColumnSpan="3"
				Content="Tip"
				FontSize="16"
				Style="{StaticResource TitleLabelStyle}" />
			<Button
				Grid.Row="0"
				Grid.Column="3"
				Command="{Binding Commands[CloseCommand]}"
				CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"
				Style="{StaticResource CloseButtonStyle}">
				<Path Style="{StaticResource TileClosePathStyle}" />
			</Button>

			<Border
				Grid.Row="1"
				Grid.Column="0"
				Grid.ColumnSpan="4"
				Style="{StaticResource BorderStyle}">

				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="10" />
						<RowDefinition />
						<RowDefinition Height="70" />
					</Grid.RowDefinitions>

					<StackPanel
						Grid.Row="1"
						HorizontalAlignment="Center"
						VerticalAlignment="Center"
						Orientation="Horizontal">
						<Image
							Source="{Binding InfoImage}"
							Style="{StaticResource MessageImageStyle}" />
						<TextBlock
							Margin="20,10"
							FontSize="16"
							Foreground="White"
							Text="{x:Static res:LanguageResource.Text_ExaminationDoorOpen}" />
					</StackPanel>

					<Button
						x:Name="btnCancel"
						Grid.Row="2"
						Click="BtnCancel_OnClick"
						Content="{x:Static res:LanguageResource.Content_Cancel}"
						Style="{StaticResource CancelButtonStyle}" />
				</Grid>

			</Border>
		</Grid>
	</Border>
</Window>
