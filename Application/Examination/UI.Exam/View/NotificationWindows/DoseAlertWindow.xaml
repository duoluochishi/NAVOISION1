<Window
    x:Class="NV.CT.UI.Exam.View.DoseAlertWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"   
    xmlns:res="clr-namespace:NV.CT.Language;assembly=NV.CT.Language"
    xmlns:md="clr-namespace:MaterialDesignThemes.Wpf.Controls.MarkableTextBox;assembly=MaterialDesignThemes.Wpf"
    Width="750" Height="500" Background="Transparent" AllowsTransparency="True" ResizeMode="NoResize"
    WindowStyle="None"  mc:Ignorable="d">
    <Window.Resources>
        <SolidColorBrush x:Key="ButtonBackColor" Color="#5a5a89" Opacity="0.28"/>
        <SolidColorBrush x:Key="ButtonFontColor" Color="White"  />
        <Style x:Key="BoundingBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#3c4354"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#5a5a89"/>
            <Setter Property="CornerRadius" Value="5"/>
        </Style>
        <Style x:Key="BorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#5a5a89"/>
            <Setter Property="Opacity" Value="0.3"/>
            <Setter Property="CornerRadius" Value="5 5 0 0"/>
        </Style>
        <Style x:Key="TilePathStyle" TargetType="{x:Type Path}" BasedOn="{StaticResource DoseConfig}">
            <Setter Property="Margin" Value="5 0 0 0"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Fill" Value="White"/>
            <Setter Property="Height" Value="22"/>
            <Setter Property="Width" Value="20"/>
            <Setter Property="Stretch" Value="Fill" />
        </Style>
        <Style x:Key="TileTextBlockStyle" TargetType="TextBlock" BasedOn="{StaticResource  MaterialDesignTextBlock}">
            <Setter Property="Margin" Value="5 0 0 0"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="16px"/>
            <Setter Property="Foreground" Value="{StaticResource BaseForegroundColor}" />
        </Style>     
        <Style x:Key="MessageImageStyle" TargetType="Image">
            <Setter Property="Margin" Value="25 0 0 23"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Opacity" Value="0.9"/>
        </Style>
        <Style x:Key="MessageInfoLabelStyle" TargetType="Label">
            <Setter Property="Margin" Value="0 0 10 0"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="MessageInfoStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="175"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="LineHeight" Value="18"/>
            <Setter Property="TextWrapping" Value="WrapWithOverflow"/>
            <Setter Property="LineHeight" Value="18"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="2 0 0 0"/>
        </Style>
        <Style x:Key="OKButtonStyle" TargetType="Button" BasedOn="{StaticResource  MaterialDesignRaisedButton}">
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="37"/>
            <Setter Property="Margin" Value="15 0 115 0"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Content" Value="{x:Static res:LanguageResource.Content_OK}"/>
            <Setter Property="Background" Value="{StaticResource ButtonBackColor}"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style x:Key="CancelButtonStyle" TargetType="Button" BasedOn="{StaticResource  MaterialDesignRaisedButton}">
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="37"/>
            <Setter Property="Margin" Value="15 0 15 0"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Content" Value="{x:Static res:LanguageResource.Content_Cancel}"/>
            <Setter Property="Background" Value="{StaticResource ButtonBackColor}"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>
    <Border CornerRadius="10" Style="{StaticResource WindowsBorder}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36" />
                <RowDefinition Height="*" />
                <RowDefinition Height="35" />
                <RowDefinition Height="80" />
                <RowDefinition Height="35" />
                <RowDefinition Height="55" />
            </Grid.RowDefinitions>
            <Border Style="{StaticResource BorderStyle}" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="35"/>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Path Grid.Column="0" Style="{StaticResource TilePathStyle}"/>
                <TextBlock Grid.Column="1" Style="{StaticResource TileTextBlockStyle}" Text="{x:Static res:LanguageResource.Content_DoseAlert}" />
                <Button Grid.Column="2" Style="{StaticResource TileCloseButtonStyle}" Command="{Binding  Commands[CloseCommand]}" CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}">
                    <Path Style="{StaticResource TileClosePathStyle}" />
                </Button>
            </Grid>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="165" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Image Style="{StaticResource MessageImageStyle}" Source="pack://application:,,,/NV.CT.UI.Controls;component/Images/Scanning.png" />
                <StackPanel Orientation="Vertical" Grid.Column="1">
                    <TextBlock Style="{StaticResource MessageInfoStyle}" Text="{x:Static res:LanguageResource.Text_DoseAlertValueExceeded}" Height="35" Margin="0 5 0 0"/>                   
                     <TextBlock Grid.Column="1" Style="{StaticResource MessageInfoStyle}" Text="{Binding Message}" />                  
                    <TextBlock Style="{StaticResource MessageInfoStyle}" Height="35" Text="{x:Static res:LanguageResource.Text_ReconsiderCurrentProtocolSetting}" />
                </StackPanel>
            </Grid>
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="165" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Style="{StaticResource MessageInfoStyle}" Text="{x:Static res:LanguageResource.Text_UserName_Mandatory}"  Grid.Column="0"  Height="36" Margin="15 0 0 0"/>
                <md:MarkableTextBox Grid.Column="1" Height="35" FontSize="14px" VerticalContentAlignment="Center" Margin="0 0 5 0" Text="{Binding UserName,Mode=TwoWay}"></md:MarkableTextBox>
            </Grid>
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="165" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Style="{StaticResource MessageInfoStyle}" Text="{x:Static res:LanguageResource.Text_DiagnosticReason}"  Grid.Column="0"  Height="35" Margin="15 0 0 0"/>
                <md:MarkableTextBox Grid.Column="1" Height="50"  FontSize="14px" VerticalContentAlignment="Center" Margin="0 0 5 0" Text="{Binding DiagnosticReason,Mode=TwoWay}" MaxLength="64"></md:MarkableTextBox>
            </Grid>
            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="165" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Style="{StaticResource MessageInfoStyle}" Text="{x:Static res:LanguageResource.Text_Password}"  Grid.Column="0" Height="36" Margin="15 0 0 0"/>
                <PasswordBox x:Name="DoseAlertPasswordBox" Grid.Column="1" FontSize="14" Height="35" VerticalContentAlignment="Center" Margin="0 0 5 0" PasswordChar="*" MaxLength="32">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="PasswordChanged">
                            <i:InvokeCommandAction Command="{Binding Commands[PasswordChangedCommand]}" CommandParameter="{Binding ElementName=DoseAlertPasswordBox}" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </PasswordBox>
            </Grid>
            <Grid Grid.Row="5">
                <Button Style="{StaticResource OKButtonStyle}" Command="{Binding  Commands[LoadCommand]}" CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"/>
                <Button Style="{StaticResource CancelButtonStyle}" Command="{Binding  Commands[CloseCommand]}" CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}"/>
            </Grid>
        </Grid>
    </Border>
</Window>