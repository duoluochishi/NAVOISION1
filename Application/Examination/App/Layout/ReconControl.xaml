<UserControl
    x:Class="NV.CT.Examination.Layout.ReconControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:res="clr-namespace:NV.CT.Language;assembly=NV.CT.Language"
    xmlns:view="clr-namespace:NV.CT.UI.Exam.View;assembly=NV.CT.UI.Exam"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style
            x:Key="BaseLiftMargin"
            TargetType="{x:Type Control}">
            <Setter Property="Margin" Value="5,0,0,0" />
        </Style>
        <Style
            x:Key="PanelTitle"
            BasedOn="{StaticResource MaterialDesignLabel}"
            TargetType="{x:Type Label}">
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Margin" Value="19,8,0,2" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="Foreground" Value="#FFC6C6EC" />
        </Style>
        <Style
            x:Key="HighlightRectangle"
            TargetType="{x:Type Rectangle}">
            <Setter Property="VerticalAlignment" Value="Bottom" />
            <Setter Property="Stroke" Value="#3044D9" />
            <Setter Property="Height" Value="3" />
            <Setter Property="StrokeThickness" Value="3" />
            <Setter Property="Width" Value="100" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="Margin" Value="19,0,0,10" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Width" Value="80" />
        </Style>
        <Style
            x:Key="BorderStyle" BasedOn="{StaticResource WindowsBorder}"
            TargetType="{x:Type Border}">
            <Setter Property="CornerRadius" Value="10" />           
        </Style>
    </UserControl.Resources>
    <AdornerDecorator>
        <Grid Background="#05050F">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="300" />
            </Grid.ColumnDefinitions>

            <!--  左侧图像显示和Toolbar  -->
            <Border
                Grid.Column="0"
                Margin="10,0,0,0"  
                BorderThickness="0"
                Style="{StaticResource BorderStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="5" />
                        <ColumnDefinition Width="80" />
                    </Grid.ColumnDefinitions>

                    <Border
                        Grid.Column="0"
                        Background="Black"                       
                        Style="{StaticResource BorderStyle}">
                        <TextBlock
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Text="new image control" />
                    </Border>

                    <Border
                        Grid.Column="2"
                        Background="Black"                      
                        Style="{StaticResource BorderStyle}">                        
                        <view:ToolsControl Style="{StaticResource BaseLiftMargin}" />
                    </Border>
                </Grid>
            </Border>

            <!--  右侧操作  -->
            <Border
                Grid.Column="2"
                Background="#2A2A47"
                BorderThickness="0"
                CornerRadius="10"
                Style="{StaticResource WindowsBorder}">
                
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="1*" />
                        <RowDefinition Height="1*" />
                    </Grid.RowDefinitions>

                    <!--  Recon Ranges  -->
                    <Grid Grid.Row="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="50" />
                            <RowDefinition Height="40" />
                            <RowDefinition Height="40" />
                        </Grid.RowDefinitions>

                        <Label
                            Grid.Row="0"
                            Grid.Column="0"
                            Content="{x:Static res:LanguageResource.Content_ReconRanges}"
                            Style="{StaticResource PanelTitle}" />
                        <Rectangle
                            Grid.Row="0"
                            Grid.Column="0"
                            Style="{StaticResource HighlightRectangle}" />
                    </Grid>

                    <!--  Plan Basis  -->
                    <Grid Grid.Row="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="50" />
                            <RowDefinition Height="40" />
                            <RowDefinition Height="40" />
                        </Grid.RowDefinitions>

                        <Label
                            Grid.Row="0"
                            Grid.Column="0"
                            Content="{x:Static res:LanguageResource.Content_PlanBasis}"
                            Style="{StaticResource PanelTitle}" />
                        <Rectangle
                            Grid.Row="0"
                            Grid.Column="0"
                            Width="80"
                            Style="{StaticResource HighlightRectangle}" />
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </AdornerDecorator>
</UserControl>
