<UserControl
    x:Class="NV.CT.Examination.View.ScanMainControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:view="clr-namespace:NV.CT.UI.Exam.View;assembly=NV.CT.UI.Exam"
    d:DesignHeight="950"
    d:DesignWidth="1950"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style
            x:Key="BaseLiftMargin"
            TargetType="{x:Type Control}">
            <Setter Property="Margin" Value="5,0,0,0" />
        </Style>
        <Style
            x:Key="BaseRightStackPanel"
            TargetType="{x:Type StackPanel}">
            <Setter Property="Orientation" Value="Vertical" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="VerticalAlignment" Value="Stretch" />
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="True">
                    <Setter Property="Visibility" Value="Visible" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Visibility" Value="Collapsed" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ContentControlStyle" TargetType="{x:Type ContentControl}">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
        </Style>
    </UserControl.Resources>
    <AdornerDecorator>
        <Grid Background="{StaticResource MaterialDesignBackground}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="340" />
                <ColumnDefinition Width="0" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="5" />
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0"  CornerRadius="10" Style="{StaticResource WindowsBorder}" Margin="0 5 0 5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="230" />
                        <RowDefinition Height="100" />
                        <RowDefinition Height="100" />
                    </Grid.RowDefinitions>
                    <view:TaskListControl Grid.Row="0" />
                    <view:BodyPositionControl Grid.Row="1"/>
                    <view:MessageTooltipControl Grid.Row="2" />
                    <view:ScanControlsControl Grid.Row="3" />
                </Grid>
            </Border>
            <Border Grid.Column="2">
                <ContentControl x:Name="LayoutContainer"  Style="{StaticResource ContentControlStyle}" />
            </Border>
        </Grid>
    </AdornerDecorator>
</UserControl>