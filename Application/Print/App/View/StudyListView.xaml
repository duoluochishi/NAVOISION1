<UserControl x:Class="NV.CT.Print.View.StudyListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NV.CT.Print.View"
             mc:Ignorable="d" 
             xmlns:wpf="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:res="clr-namespace:NV.CT.Language;assembly=NV.CT.Language"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="DatagridCenterHeader" BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}" TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="HorizontalContentAlignment" Value="Center" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="56"/>  <!-- 0 //StudyList Section-->
            <RowDefinition Height="180"/> <!-- 1 //StudyList--> 
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <TextBlock x:Name="lblStudyListSection" Text="{x:Static res:LanguageResource.Content_StudyInfo}" 
                           Style="{StaticResource SectionTitleTextBlock}" Margin="8,0,0,0"/>
            <Rectangle Width="{Binding ElementName=lblStudyListSection,Path=ActualWidth}"
                           Style="{StaticResource SectionTitleUnderlineRectangle}" Margin="8,0,0,14"/>
        </Grid>
        <Grid Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <DataGrid x:Name="dgBodyParts" ItemsSource="{Binding StudyBodyPartModelList}" Style="{StaticResource CommonDataGrid}" HorizontalAlignment="Stretch" Margin="8,0,1,0">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="{x:Static res:LanguageResource.Header_BodyPart}" Width="114" HeaderStyle="{StaticResource DatagridCenterHeader}">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock HorizontalAlignment="Center" Text="{Binding BodyPart}" Style="{StaticResource DataGridContentTextBlock}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="{x:Static res:LanguageResource.Header_EndDate}" Width="*" MinWidth="110" HeaderStyle="{StaticResource DatagridCenterHeader}">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock HorizontalAlignment="Center" Text="{Binding ReconEndDate, StringFormat='yyyy/M/dd HH:mm:ss'}" Style="{StaticResource DataGridContentTextBlock}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>

</UserControl>
