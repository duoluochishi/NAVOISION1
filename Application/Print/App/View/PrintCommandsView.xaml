<UserControl x:Class="NV.CT.Print.View.PrintCommandsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NV.CT.Print.View"
             xmlns:wpf="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:res="clr-namespace:NV.CT.Language;assembly=NV.CT.Language"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Style="{StaticResource InfomationGrid}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="36*"/>
                <ColumnDefinition Width="64*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{x:Static res:LanguageResource.Content_Display}" Style="{StaticResource InputLabelTextBlock}" Margin="8,0,0,0" />
            <WrapPanel Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <RadioButton Name="rdoBrowser" GroupName="Type" IsChecked="{Binding IsBrowserChecked, Mode=TwoWay}" Content="{x:Static res:LanguageResource.Content_Browser}" Margin="0,0,0,0" />
                <RadioButton Name="rdoOverview" GroupName="Type" IsChecked="{Binding IsOverviewChecked, Mode=TwoWay}" Content="{x:Static res:LanguageResource.Content_Overview}" Margin="30,0,0,0"/>
            </WrapPanel>
        </Grid>
        <Grid Grid.Row="1" Style="{StaticResource InfomationGrid}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Button Command="{Binding Commands[PrintCommand]}" Content="{x:Static res:LanguageResource.Content_Print}" IsEnabled="{Binding IsPrintEnabled, Mode=TwoWay}"
                    Style="{StaticResource CommonButton}" Width="140" Margin="7,0,0,0" />
        </Grid>

    </Grid>
</UserControl>
