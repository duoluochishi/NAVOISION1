<Window x:Class="NV.CT.PatientBrowser.View.English.FiltrationWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"   
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"      
    xmlns:res="clr-namespace:NV.CT.Language;assembly=NV.CT.Language"
    Width="350" Height="280" Background="Transparent" ResizeMode="NoResize" AllowsTransparency="True"
    WindowStyle="None" mc:Ignorable="d">
    <Window.Resources>
    </Window.Resources>
    <Border CornerRadius="10" Style="{StaticResource WindowsBorder}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="35" />
                <RowDefinition Height="*" />               
            </Grid.RowDefinitions>
            <Border Style="{StaticResource WindowContentBorder}" />
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <TextBlock Style="{StaticResource TileTextBlock}" Text="{x:Static res:LanguageResource.Content_Filter}" />
                <Button Grid.Column="1" Style="{StaticResource TileCloseButtonStyle}" ToolTip="{x:Static res:LanguageResource.Content_Close}"
                        Command="{Binding  Commands[CloseCommand]}" 
                        CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}">
                    <Path Style="{StaticResource TileClosePathStyle}" />
                </Button>
            </Grid>
            <StackPanel Grid.Row="1" Orientation="Vertical">
                <TextBlock Style="{StaticResource MessageInfoTextBlock}" Text="{x:Static res:LanguageResource.Content_DefaultSearchScope}"/>
                <RadioButton Content="{x:Static res:LanguageResource.Content_Today}" GroupName="RearchType" IsChecked="{Binding IsToday,Mode=TwoWay}" Margin="10,0,0,0"/>
                <RadioButton Content="{x:Static res:LanguageResource.Content_Yesterday}" GroupName="RearchType" IsChecked="{Binding IsYesterday,Mode=TwoWay}" Margin="10,0,0,0"/>
                <RadioButton Content="{x:Static res:LanguageResource.Content_DayBeforeYesterday}" GroupName="RearchType" IsChecked="{Binding IsDayBeforeYesterday,Mode=TwoWay}" Margin="10,0,0,0"/>
                <RadioButton Content="{x:Static res:LanguageResource.Content_WithinTheLast7Days}" GroupName="RearchType" IsChecked="{Binding IsLast7Days,Mode=TwoWay}" Margin="10,0,0,0"/>
                <RadioButton Content="{x:Static res:LanguageResource.Content_WithinTheLast30Days}" GroupName="RearchType" IsChecked="{Binding IsLast30Days,Mode=TwoWay}" Margin="10,0,0,0"/>
                <RadioButton Content="{x:Static res:LanguageResource.Content_All}" GroupName="RearchType" IsChecked="{Binding IsAll,Mode=TwoWay}" Margin="10,0,0,0"/>              
            </StackPanel>                
        </Grid>
    </Border>
</Window>