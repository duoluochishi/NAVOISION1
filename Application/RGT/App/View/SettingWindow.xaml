<Window
    x:Class="NV.CT.RGT.View.SettingWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:NV.CT.RGT.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="SettingWindow"
    Width="1000"
    Height="650"
    AllowsTransparency="True"
    Background="Transparent"
    ResizeMode="NoResize"
    WindowStyle="None"
    mc:Ignorable="d">

    <Window.Resources>
        <SolidColorBrush x:Key="SExposuresIconFillBrush">#FFA63F</SolidColorBrush>
        <SolidColorBrush x:Key="BorderColor">#16162B</SolidColorBrush>
        <Style
            x:Key="NBorderBaseStyle"
            TargetType="{x:Type Border}">
            <Setter Property="Background" Value="{DynamicResource BorderColor}" />
            <Setter Property="CornerRadius" Value="10,10,10,10" />
            <Setter Property="BorderBrush" Value="#5a5a89" />
            <Setter Property="BorderThickness" Value="0.5" />
        </Style>
        <Style
            x:Key="TileClosePathStyle"
            BasedOn="{StaticResource CloseGeometry}"
            TargetType="Path">
            <Setter Property="Fill" Value="{StaticResource TextBoxBrush}" />
            <Setter Property="Height" Value="20" />
            <Setter Property="Width" Value="20" />
            <Setter Property="Stretch" Value="Fill" />
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="4,8,8,8" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontFamily" Value="Microsoft YaHei" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </Window.Resources>

    <Grid>
        <Border
            Background="#14142B"
            BorderBrush="#5A5A89"
            CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition />
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition />
                    <ColumnDefinition Width="10" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Border
                    Grid.ColumnSpan="5"
                    Background="#5A5A89"
                    CornerRadius="5, 5, 0, 0"
                    Opacity="0.3" />

                <Label
                    Grid.Row="0"
                    Grid.Column="0"
                    Grid.ColumnSpan="4"
                    Margin="10,0,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    FontSize="20"
                    Foreground="White">
                    Setting
                </Label>
                <Button
                    Grid.Row="0"
                    Grid.Column="3"
                    Width="22"
                    Margin="0,0,10,0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Click="ButtonBase_OnClick"
                    Style="{StaticResource MaterialDesignIconButton}">
                    <Path Style="{StaticResource TileClosePathStyle}" />
                </Button>

                <Grid
                    Grid.Row="1"
                    Grid.Column="0"
                    Grid.ColumnSpan="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel
                        Grid.Column="0"
                        Margin="20,0,0,0"
                        HorizontalAlignment="Left"
                        Orientation="Vertical">
                        <TextBlock Text="Protocol" />
                        <TextBlock Text="KV" />
                        <TextBlock Text="mA" />
                        <TextBlock Text="Patient Position" />
                        <TextBlock Text="Scan Length" />
                        <TextBlock Text="CTDlvol" />
                        <TextBlock Text="DLP" />
                        <!--<Button
                            Width="100"
                            Height="37"
                            HorizontalAlignment="Center"
                            Command="{Binding Commands[CancelCommand]}"
                            CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}">
                            Cancel
                        </Button>-->
                    </StackPanel>

                    <Grid Grid.Column="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*" />
                            <RowDefinition Height="1*" />
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="100" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="100" />
                            </Grid.RowDefinitions>

                            <TextBlock
                                Grid.Row="0"
                                Text="Connection" />

                            <Image
                                Grid.Row="1"
                                Cursor="Hand"
                                Source="../Resources/icons/network.png" />

                            <TextBlock
                                Grid.Row="2"
                                Text="Connection Ok" />
                        </Grid>

                        <Grid Grid.Row="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="100" />
                                <RowDefinition Height="100" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <TextBlock
                                Grid.Row="0"
                                Text="Version" />

                            <TextBlock
                                Grid.Row="1"
                                Text="V1.0" />
                        </Grid>
                    </Grid>

                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
