<UserControl
	x:Class="NV.CT.RGT.Layout.PatientBrowser"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:res="clr-namespace:NV.CT.Language;assembly=NV.CT.Language"
	xmlns:view="clr-namespace:NV.CT.RGT.View"
	d:DesignHeight="1080"
	d:DesignWidth="1920"
	Background="#0c0c27"
	FontSize="25"
	mc:Ignorable="d">
	<Grid>

		<!--SelectionChanged="Selector_OnSelectionChanged"-->

		<DataGrid
			x:Name="DataGrid"
			HorizontalAlignment="Stretch"
			AutoGenerateColumns="False"
			Background="{StaticResource Background}"
			ColumnHeaderHeight="70"
			FontSize="20"
			Foreground="LightGray"
			GridLinesVisibility="Horizontal"
			HorizontalScrollBarVisibility="Disabled"
			IsReadOnly="True"
			ItemsSource="{Binding Studies}"
			RowHeight="80"
			SelectedItem="{Binding SelectedStudy, Mode=TwoWay}"
			SelectionUnit="FullRow"
			VerticalScrollBarVisibility="Auto">
			<DataGrid.Columns>

				<DataGridTemplateColumn
					Width="3*"
					MinWidth="110"
					Header="{x:Static res:LanguageResource.Content_Name}">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<TextBlock
								HorizontalAlignment="Left"
								VerticalAlignment="Center"
								Text="{Binding PatientName}" />
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>

				<DataGridTemplateColumn
					Width="3*"
					MinWidth="110"
					Header="{x:Static res:LanguageResource.Content_PatientId}">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<TextBlock
								HorizontalAlignment="Left"
								VerticalAlignment="Center"
								Text="{Binding PatientId}" />
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>


				<DataGridTemplateColumn
					Width="1*"
					MinWidth="110"
					Header="{x:Static res:LanguageResource.Content_Sex}">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<TextBlock
								HorizontalAlignment="Left"
								VerticalAlignment="Center"
								Text="{Binding Sex}" />
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>


				<DataGridTemplateColumn
					Width="1*"
					MinWidth="110"
					Header="{x:Static res:LanguageResource.Content_Age}">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<TextBlock
								HorizontalAlignment="Left"
								VerticalAlignment="Center"
								Text="{Binding Age}" />
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>


				<!--<DataGridTemplateColumn
                    Width="auto"
                    MinWidth="110"
                    Header="BodyPart">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Text="{Binding BodyPart}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>-->

				<DataGridTemplateColumn
					Width="2*"
					MinWidth="300"
					Header="{x:Static res:LanguageResource.Header_CreatedTime}">
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<TextBlock
								HorizontalAlignment="Left"
								VerticalAlignment="Center"
								Text="{Binding StudyDate, StringFormat='yyyy/M/dd HH:mm:ss'}" />
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>



			</DataGrid.Columns>
		</DataGrid>
	</Grid>
</UserControl>
