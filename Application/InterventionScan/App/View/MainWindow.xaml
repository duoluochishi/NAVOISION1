<Window
    x:Class="NV.CT.InterventionScan.View.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:view="clr-namespace:NV.CT.InterventionScan.View"
    Background="Transparent" WindowState="Maximized" WindowStyle="SingleBorderWindow" mc:Ignorable="d">
    <Window.Resources>
        <Style x:Key="BorderStyle" TargetType="{x:Type Border}" BasedOn="{StaticResource WindowsBorder}">
            <Setter Property="CornerRadius" Value="10"/>
        </Style>
    </Window.Resources>
    <Border Style="{StaticResource BorderStyle}">
        <view:InterventionScanControl/>
    </Border>
</Window>