<UserControl x:Class="NV.CT.LogManagement.View.English.LogViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:md="clr-namespace:MaterialDesignThemes.Wpf.Controls.MarkableTextBox;assembly=MaterialDesignThemes.Wpf"
             xmlns:local="clr-namespace:NV.CT.LogManagement.View.English"
             xmlns:wpf="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:res="clr-namespace:NV.CT.Language;assembly=NV.CT.Language"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Style="{StaticResource GroupedBorder}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid>
            <Grid.RowDefinitions>
                <!-- Search criteria -->
                <RowDefinition Height="80"/>
                <!-- Separator -->
                <RowDefinition Height="6"/>
                <!-- Search result -->
                <RowDefinition Height="600"/>
                <!-- Splitter -->
                <RowDefinition Height="4"/>
                <!-- Original log viewer -->
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <local:LogViewerSearchCriteria/>
            </Grid>
            <Grid Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="12,0,12,1">
                <Separator Background="#5A5A89" >
                    <Separator.LayoutTransform>
                        <RotateTransform Angle="0"></RotateTransform>
                    </Separator.LayoutTransform>
                </Separator>
            </Grid>
            <Grid Grid.Row="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <local:LogViewerSearchResult/>
            </Grid>
            <GridSplitter Grid.Row="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Style="{StaticResource MaterialDesignGridSplitter}" Background="LightBlue"/>
            <Grid Grid.Row="4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,0,0,4">
                <local:LogViewerOriginalLocator/>
            </Grid>
        </Grid>
    </Border>
</UserControl>
