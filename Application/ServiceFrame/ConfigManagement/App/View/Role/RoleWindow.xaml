<Window
    x:Class="NV.CT.ConfigManagement.View.RoleWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:md="clr-namespace:MaterialDesignThemes.Wpf.Controls.MarkableTextBox;assembly=MaterialDesignThemes.Wpf"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:res="clr-namespace:NV.CT.Language;assembly=NV.CT.Language"    
    Width="860" Height="750" Background="Transparent" AllowsTransparency="True" ResizeMode="NoResize" WindowStyle="None" mc:Ignorable="d">
    <Window.Resources>
        <Style x:Key="BorderStyle" TargetType="{x:Type Border}">
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="True">
                    <Setter Property="Visibility" Value="Visible" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Visibility" Value="Collapsed" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="TitleBorderStyle" TargetType="{x:Type Border}">
            <Setter Property="Opacity" Value="0.3"/>
            <Setter Property="Background" Value="{StaticResource BaseTabBackBrush}"/>
            <Setter Property="CornerRadius" Value="5 5 0 0" />
        </Style>
        <Style x:Key="TitlePathStyle" BasedOn="{StaticResource IconSavea}" TargetType="{x:Type Path}">
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
            <Setter Property="Stretch" Value="Fill"/>
            <Setter Property="Fill" Value="{StaticResource TextBoxBrush}"/>
        </Style>
        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource  MaterialDesignIconButton}">
            <Setter Property="Width" Value="22"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="ComboBox" BasedOn="{StaticResource NanoComboBoxStyle}">
            <Setter Property="DisplayMemberPath" Value="Value"/>
            <Setter Property="SelectedValuePath" Value="Key"/>
        </Style>
        <Style x:Key="ProtocolTileLabelStyle" BasedOn="{StaticResource MaterialDesignLabel}" TargetType="Label">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Foreground" Value="#FFC6C6EC"/>
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style x:Key="ProtocolTileRectangleStyle" TargetType="Rectangle">
            <Setter Property="StrokeThickness" Value="3"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Stroke" Value="#3044D9" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Bottom" />
            <Setter Property="Height" Value="3" />
            <Setter Property="Width" Value="70" />
            <Setter Property="Margin" Value="2,0,0,10" />
        </Style>
        <Style x:Key="ProtocolLabelStyle" BasedOn="{StaticResource MaterialDesignLabel}" TargetType="Label">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style x:Key="ProtocolTextBoxStyle" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}" TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource BaseForegroundColor}"/>
            <Setter Property="VerticalAlignment" Value="Top" />
            <Setter Property="AcceptsReturn" Value="True" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="Height" Value="80" />
            <Setter Property="Margin" Value="2" />
        </Style>
        <Style x:Key="ProtocolStackPanelStyle" TargetType="StackPanel">
            <Setter Property="Orientation" Value="Horizontal"/>
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style x:Key="SaveButtonStyle" TargetType="Button" BasedOn="{StaticResource  NanoButtonStyle}">
            <Setter Property="Width" Value="160"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0,0,15,0"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>
        <Style x:Key="ClosedButtonStyle" TargetType="Button" BasedOn="{StaticResource  NanoButtonStyle}">
            <Setter Property="Width" Value="160"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="15,0,0,0"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
        <Style x:Key="SuffTextBlockStyle" BasedOn="{StaticResource NanoTextBlockStyle}" TargetType="{x:Type TextBlock}">
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="#FFA63F" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style
            x:Key="ProtocolListBoxItemStyle"
            BasedOn="{StaticResource MaterialDesignListBoxItem}"
            TargetType="{x:Type ListViewItem}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="#5a5a89" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
        </Style>
        <Style
            x:Key="ListBoxViewStyle"
            BasedOn="{StaticResource MaterialDesignListBox}"
            TargetType="{x:Type ListView}">
            <Setter Property="ItemContainerStyle" Value="{StaticResource ProtocolListBoxItemStyle}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="MaxHeight" Value="490" />
            <Setter Property="MinHeight" Value="30" />
            <Setter Property="Margin" Value="18,5,0,0" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="VerticalAlignment" Value="Top" />
            <Setter Property="HorizontalAlignment" Value="Stretch" />
        </Style>
        <Style
              x:Key="ListBoxItemStyle"
              TargetType="{x:Type ListBoxItem}">
            <Setter Property="Foreground" Value="#5a5a89" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="MinHeight" Value="22" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Padding" Value="3,0,0,0" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <ContentPresenter
                      Margin="{TemplateBinding Padding}"
                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border Style="{StaticResource WindowsBorder}" CornerRadius="10" >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36" />
                <RowDefinition Height="*" />
                <RowDefinition Height="55" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="40" />
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Border  Grid.Column="0" Grid.ColumnSpan="3" Style="{StaticResource TitleBorderStyle}"/>
                <Path  Grid.Column="0" Style="{StaticResource TitlePathStyle}" />
                <Label  Grid.Column="1" Grid.ColumnSpan="2" Content="Role" FontSize="16" Style="{StaticResource TitleLabelStyle}" />
                <Button  Grid.Column="2" Command="{Binding Commands[CloseCommand]}" CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" Style="{StaticResource CloseButtonStyle}">
                    <Path Style="{StaticResource TileClosePathStyle}" />
                </Button>
            </Grid>
            <StackPanel Grid.Row="1" Orientation="Vertical" Margin="10 0 0 0">
                <StackPanel Orientation="Vertical">
                    <Label Grid.ColumnSpan="2" Name="lblTabTitle" 
                        FontSize="16" FontWeight="Bold" 
                        Margin="0,8,0,0" 
                        Content="Role info"
                        HorizontalAlignment="Left" 
                        VerticalAlignment="Center" 
                        Height="34" 
                        Foreground="#FFC6C6EC" />
                    <Rectangle Grid.Column="0" 
                        VerticalAlignment="Bottom" Stroke="#3044D9" 
                        Height="3" StrokeThickness="3" 
                        Width="{Binding ElementName=lblTabTitle,Path=ActualWidth}" HorizontalAlignment="Left"
                        Margin="0,0,0,-5" 
                        SnapsToDevicePixels="True" Visibility="Visible" />
                </StackPanel>

                <Grid Margin="0 15 0 0" Height="42">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="40" />
                        <ColumnDefinition Width="135" />
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="80" />
                        <ColumnDefinition Width="150" />
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="80" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Style="{StaticResource SuffTextBlockStyle}" Text="*" />
                        <Label Style="{StaticResource ProtocolLabelStyle}" Content="Name"/>
                    </StackPanel>
                    <md:MarkableTextBox  Grid.Column="2"  FontSize="14px" MaxLength="32" VerticalContentAlignment="Center" Margin="0 0 5 0" Text="{Binding CurrentRole.Name,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" />

                    <Label Grid.Column="4"  VerticalAlignment="Center" Content="Description"/>
                    <md:MarkableTextBox  Grid.Column="5" FontSize="14px" VerticalContentAlignment="Center" Margin="0 0 5 0" Text="{Binding CurrentRole.Description,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" />
                </Grid>

                <StackPanel Margin="0 15 0 0" Orientation="Vertical">
                    <Label FontSize="15" FontWeight="Bold" 
                       Margin="0,8,0,0" 
                       Content="Permissions list" 
                       HorizontalAlignment="Left" VerticalAlignment="Center" 
                       x:Name="lblTabTitle1"  Height="34" 
                       Foreground="#FFC6C6EC" />
                    <Rectangle VerticalAlignment="Bottom" 
                       Stroke="#3044D9" Height="3" 
                       StrokeThickness="3" Width="{Binding ElementName=lblTabTitle1,Path=ActualWidth}" 
                       HorizontalAlignment="Left" 
                       Margin="0,0,0,-5" 
                       SnapsToDevicePixels="True" />
                </StackPanel>
                <ListView
                    x:Name="ListView"                   
                    IsSynchronizedWithCurrentItem="True"
                    ItemsSource="{Binding AllPermissions, Mode=TwoWay}"                    
                    ScrollViewer.CanContentScroll="False"                   
                    Style="{StaticResource ListBoxViewStyle}"
                    Background="Transparent">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="42"></RowDefinition>
                                    <RowDefinition Height="*" MinHeight="36"></RowDefinition>
                                </Grid.RowDefinitions>
                                <Grid Grid.Row="0" Height="42" VerticalAlignment="Top" Margin="10,5,0,0">
                                    <Label FontSize="14" FontWeight="Bold" 
                                                   Margin="0,8,0,10" 
                                                   Content="{Binding GroupName}" 
                                                   HorizontalAlignment="Left" VerticalAlignment="Center" 
                                                   x:Name="lblTabTitle" 
                                                   Grid.ColumnSpan="2" Height="34" 
                                                   Foreground="#FFC6C6EC" />
                                    <Rectangle VerticalAlignment="Bottom" Grid.Column="0" 
                                                   Stroke="#3044D9" Height="3" 
                                                   StrokeThickness="3" Width="{Binding ElementName=lblTabTitle,Path=ActualWidth}" 
                                                   HorizontalAlignment="Left" 
                                                   Margin="0,10,0,10" 
                                                   SnapsToDevicePixels="True" />
                                </Grid>
                                <Grid Grid.Row="1">
                                    <ListView Margin="10,-5,0,-5"  HorizontalAlignment="Stretch" Background="Transparent"
                                            ItemsSource="{Binding PermissionViewModels, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
                                        <ListView.ItemContainerStyle>
                                            <Style BasedOn="{StaticResource ListBoxItemStyle}" TargetType="{x:Type ListBoxItem}" />
                                        </ListView.ItemContainerStyle>
                                        <ListView.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel Margin="0"
                                                        HorizontalAlignment="Left"
                                                        VerticalAlignment="Stretch"                                                       
                                                        Cursor="Hand"
                                                        MaxWidth="750"
                                                        Orientation="Horizontal">
                                                </WrapPanel>
                                            </ItemsPanelTemplate>
                                        </ListView.ItemsPanel>
                                        <ListView.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="Transparent">
                                                    <CheckBox Margin="15 5 0 0" IsChecked="{Binding IsChecked,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" Content="{Binding Name}" />
                                                </Border>
                                            </DataTemplate>
                                        </ListView.ItemTemplate>
                                    </ListView>
                                </Grid>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
            <StackPanel Grid.Row="2" Style="{StaticResource ProtocolStackPanelStyle}">
                <Button Style="{StaticResource SaveButtonStyle}" Command="{Binding Commands[SaveCommand]}" CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" Content="{x:Static res:LanguageResource.Content_Save}" />
                <Button Style="{StaticResource ClosedButtonStyle}" Command="{Binding Commands[CloseCommand]}" CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType=Window}}" Content="{x:Static res:LanguageResource.Content_Close}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>