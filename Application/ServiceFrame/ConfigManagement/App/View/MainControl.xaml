<UserControl
    x:Class="NV.CT.ConfigManagement.View.MainControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"   
    d:DesignHeight="950" d:DesignWidth="1950" mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="ContentControlStyle" TargetType="{x:Type ContentControl}">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
        </Style>
    </UserControl.Resources>
    <AdornerDecorator>
        <ContentControl x:Name="LayoutContainer" Style="{StaticResource ContentControlStyle}"/>
    </AdornerDecorator>
</UserControl>